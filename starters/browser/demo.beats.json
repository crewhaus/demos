{
  "title": "hello-browser — screenshot, find, click (target: browser)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“An agent that drives a headless Chromium via Navigate, Screenshot, FindElement, Click, and Type.” Read the summary; scroll no further.", "script": "“An agent that drives a headless Chromium via Navigate, Screenshot, FindElement, Click, and Type.” Read the summary; scroll no further. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md", "verify": { "note": "OPERATOR: the README's Run-it block is the same compile → bun add playwright → bun dist/agent.ts ladder this driver walks, so the preview and the terminal agree. Keep the preview on the summary paragraph." } },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — the planner and the grounding model are both Anthropic.", "script": "Sources demos/.env — the planner and the grounding model are both Anthropic. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The spec",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“Two lines: what it's called, and which runtime spine to compile.”", "script": "Two lines: what it's called, and which runtime spine to compile. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "Add the agent + planner model", "cue": "One model does the planning — which tool to call next, and where.", "script": "One model does the planning — which tool to call next, and where. [[pause:900ms]] Watch lines 3 through 4 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 4] },
        { "id": "spec-instructions", "label": "Write the tool contract", "cue": "“Be deliberate: ONE screenshot, find the target, click its center, ONE more to verify.”", "script": "Be deliberate: ONE screenshot, find the target, click its center, ONE more to verify. [[pause:900ms]] Watch lines 5 through 17 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [5, 17] },
        { "id": "spec-driver", "label": "Add the driver block", "cue": "Chromium, a 1024×720 viewport, and a public startUrl — the driver proxies every request and 403s loopback, so 127.0.0.1 is never an option.", "script": "Chromium, a 1024×720 viewport, and a public startUrl — the driver proxies every request and 403s loopback, so 127.0.0.1 is never an option. [[pause:900ms]] Watch lines 18 through 26 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [18, 26] },
        { "id": "spec-grounding", "label": "Name the grounding model", "cue": "FindElement's eye — it turns “the green Submit button” into center coordinates.", "script": "FindElement's eye — it turns “the green Submit button” into center coordinates. [[pause:900ms]] Watch lines 27 through 27 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [27, 27] },
        { "id": "spec-perm-mode", "label": "Open the permissions block", "cue": "mode: default — every tool has to be allowed by name, or it stops and asks.", "script": "Mode: default — every tool has to be allowed by name, or it stops and asks. [[pause:900ms]] Watch lines 28 through 30 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [28, 30] },
        { "id": "spec-perm-rules", "label": "Allow all seven tools by name", "cue": "Seven alwaysAllow rules — one per tool the bundle registers. Navigate is one of them, and it is the one people forget.", "script": "Seven alwaysAllow rules — one per tool the bundle registers. Navigate is one of them, and it is the one people forget. [[pause:900ms]] Watch lines 31 through 47 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [31, 47] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "Parse, then the IR passes the compile path skips — nothing emitted yet.", "script": "Parse, then the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The spec is sugar; this JSON is what the compiler actually sees.”", "script": "The spec is sugar; this JSON is what the compiler actually sees. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target browser (driver + groundingModel + permission rules).", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target browser (driver + groundingModel + permission rules)" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "--check asserts the shape, installs the deps, and boots it to its gate: GREEN.", "script": "--check asserts the shape, installs the deps, and boots it to its gate: GREEN. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (10 anchors); install ok; boot gated at the initial --prompt gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (10 anchors); install ok; boot gated at the initial --prompt gate", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "One file. The instructions, the model, the viewport, the startUrl — constants at the top. No YAML at runtime.", "script": "One file. The instructions, the model, the viewport, the startUrl — constants at the top. No YAML at runtime. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" },
        { "id": "prove-tools", "label": "Show what the bundle imports", "cue": "Screenshot, mouse+keyboard, vision grounding: real packages, not prompt text. Note the first one — navigate, which the codegen registers whether you asked for it or not.", "script": "Screenshot, mouse+keyboard, vision grounding: real packages, not prompt text. Note the first one — navigate, which the codegen registers whether you asked for it or not. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. 5 tool imports — navigate, screen-capture, mouse-keyboard, vision-grounding, catalog.", "action": "command", "command": "grep -n \"@crewhaus/tool\" live/dist/agent.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "5 tool imports — navigate, screen-capture, mouse-keyboard, vision-grounding, catalog" } },
        { "id": "prove-rules", "label": "Show the permission rules", "cue": "Seven alwaysAllow rules, compiled in — Navigate first, matching the seven tools the bundle just imported.", "script": "Seven alwaysAllow rules, compiled in — Navigate first, matching the seven tools the bundle just imported. [[pause:1.5s]] This is an offline proof and should complete successfully. One match (the emitted RuleSet) + 10 lines: flag/settings and all seven yaml alwaysAllow rules, Navigate through Scroll.", "action": "command", "command": "grep -n -A 10 \"permissionRules\" live/dist/agent.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "one match (the emitted RuleSet) + 10 lines: flag/settings and all seven yaml alwaysAllow rules, Navigate through Scroll. Under mode default anything NOT listed resolves to \"ask\", which a non-interactive surface collapses to a deny." } }
      ]
    },
    {
      "title": "Beat 3 · Give it a browser",
      "beats": [
        { "id": "run-playwright", "label": "Add Playwright to the bundle", "cue": "Playwright is an optional peer dep — you install it only when you want chromium.", "script": "Playwright is an optional peer dep — you install it only when you want chromium. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. @crewhaus/computer-use-driver declares playwright as an OPTIONAL peer dep, so the bundle install never pulls it.", "action": "command", "command": "bun add --cwd live/dist playwright", "verify": { "mode": "offline", "expectedExit": 0, "note": "@crewhaus/computer-use-driver declares playwright as an OPTIONAL peer dep, so the bundle install never pulls it", "produces": ["live/dist/node_modules/.bin/playwright"] } },
        { "id": "run-chromium-plan", "label": "Show which chromium it pins", "cue": "Exactly which build, and where it lands — before you spend the bandwidth.", "script": "Exactly which build, and where it lands — before you spend the bandwidth. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. Prints Chrome for Testing + Chrome Headless Shell + ffmpeg: version, install location, download URL. Downloads nothing.", "action": "command", "command": "bun live/dist/node_modules/.bin/playwright install --dry-run chromium", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints Chrome for Testing + Chrome Headless Shell + ffmpeg: version, install location, download URL. Downloads nothing." } },
        { "id": "run-chromium", "label": "Fetch the chromium build", "cue": "[manual] One-time: that exact chromium, into the shared cache.", "script": "One-time: that exact chromium, into the shared cache. [[pause:1.8s]] This step hands control to the operator for the interactive portion. ~150MB browser-binary download; run it once before the take — the verifier never runs it (the --dry-run beat above proves the invocation form).", "action": "command", "command": "bun live/dist/node_modules/.bin/playwright install chromium", "verify": { "mode": "manual", "note": "~150MB browser-binary download; run it once before the take — the verifier never runs it (the --dry-run beat above proves the invocation form)" } },
        { "id": "run-repl", "label": "Launch the bundle", "cue": "[needs-key] No --prompt and a TTY: one live browser context, turn after turn.", "script": "No --prompt and a TTY: one live browser context, turn after turn. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. 0.4.0 runs chromium HEADLESS with no knob to change it (createDriver hardcodes headless: true), so the browser window is never on camera — the JSON event stream and the model's answers are the take.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "0.4.0 runs chromium HEADLESS with no knob to change it (createDriver hardcodes headless: true), so the browser window is never on camera — the JSON event stream and the model's answers are the take" } },
        { "id": "run-look", "label": "Look at the start page", "cue": "[needs-key] The startUrl loaded before the first turn — the agent only has to look.", "script": "The startUrl loaded before the first turn — the agent only has to look. [[pause:1.2s]] The driver sends the next user turn into the active session: “Take ONE screenshot and tell me, in one sentence, exactly what this page says.” Example.com through the SSRF proxy: the answer quotes “Example Domain” / “This domain is for use in documentation examples…”.", "action": "input", "text": "Take ONE screenshot and tell me, in one sentence, exactly what this page says.", "verify": { "mode": "needs-key", "note": "example.com through the SSRF proxy: the answer quotes “Example Domain” / “This domain is for use in documentation examples…”. Point startUrl at a loopback or RFC1918 host instead and the screenshot reads `ssrf-pinning-proxy: blocked target IP …` while {\"kind\":\"navigated\"} still fires — page.goto() resolves on a non-2xx." } },
        { "id": "run-navigate", "label": "Ask it to go somewhere real", "cue": "[needs-key] Navigate is the seventh rule. Leave it out and this turn stops dead — on a REPL it asks, everywhere else it denies.", "script": "Navigate is the seventh rule. Leave it out and this turn stops dead — on a REPL it asks, everywhere else it denies. [[pause:1.2s]] The driver sends the next user turn into the active session: “Navigate to https://crewhaus.ai and take ONE screenshot.” No approval prompt.", "action": "input", "text": "Navigate to https://crewhaus.ai and take ONE screenshot.", "verify": { "mode": "needs-key", "note": "no approval prompt — the yaml alwaysAllow rule for Navigate resolves it before the ask path; a second {\"kind\":\"navigated\"} is NOT emitted (only startUrl emits one), so the proof is the model describing crewhaus.ai" } },
        { "id": "run-find", "label": "Ground an element", "cue": "[needs-key] FindElement returns a bounding box and a center — coordinates, from pixels.", "script": "FindElement returns a bounding box and a center — coordinates, from pixels. [[pause:1.2s]] The driver sends the next user turn into the active session: “Use FindElement to locate the primary call-to-action button and give me its center coordinates.” The resulting answer is the live evidence for the contract described in the first chunk.", "action": "input", "text": "Use FindElement to locate the primary call-to-action button and give me its center coordinates.", "verify": { "mode": "needs-key" } },
        { "id": "run-scroll", "label": "Act on the page", "cue": "[needs-key] Scroll is destructive; the alwaysAllow rule is why it never stops to ask.", "script": "Scroll is destructive; the alwaysAllow rule is why it never stops to ask. [[pause:1.2s]] The driver sends the next user turn into the active session: “Scroll down one screen, take ONE more screenshot, and tell me what changed.” Watch the response demonstrate the behavior while preserving the same session context.", "action": "input", "text": "Scroll down one screen, take ONE more screenshot, and tell me what changed.", "verify": { "mode": "needs-key" } },
        { "id": "run-exit", "label": "Exit the REPL", "cue": "[needs-key] “exit” returns to the shell; the browser context closes with it.", "script": "“exit” returns to the shell; the browser context closes with it. [[pause:1.2s]] The driver sends exit to close the active REPL cleanly. The terminal returns to a normal shell while the generated artifacts and session history remain available.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · scripted, not interactive",
      "beats": [
        { "id": "extra-oneshot", "label": "Run one turn, non-interactively", "cue": "[needs-key] browser_start, navigated, browser_done — JSON events on stdout. The CI shape.", "script": "Browser_start, navigated, browser_done — JSON events on stdout. The CI shape. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. One prompt, one turn, then exit — no REPL. Screenshot is in the seven rules, so nothing has to ask.", "action": "command", "command": "bun live/dist/agent.ts --prompt \"Take ONE screenshot and tell me, in one sentence, exactly what this page says.\"", "verify": { "mode": "needs-key", "note": "one prompt, one turn, then exit — no REPL. Screenshot is in the seven rules, so nothing has to ask." } },
        { "id": "extra-stdin", "label": "Or pipe the prompt in", "cue": "[needs-key] No --prompt, no TTY: the bundle reads the task off stdin — and every tool it needs is already named, so nothing stalls.", "script": "No --prompt, no TTY: the bundle reads the task off stdin — and every tool it needs is already named, so nothing stalls. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. The bundle falls back to readStdin() when --prompt is absent and stdin is not a TTY. This is the surface that cannot prompt for approval an \"ask\" here collapses to a deny.", "action": "command", "command": "echo \"Navigate to https://crewhaus.ai and take ONE screenshot.\" | bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "the bundle falls back to readStdin() when --prompt is absent and stdin is not a TTY. This is the surface that cannot prompt for approval — an \"ask\" here collapses to a deny — which is exactly why Navigate carries its own alwaysAllow rule. Navigate + Screenshot both run; the page description lands in the browser_done finalText, exit 0." } }
      ]
    }
  ]
}
