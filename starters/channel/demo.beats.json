{
  "title": "hello-channel — a Slack bot compiled into a daemon (target: channel)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“A spec in, a long-running daemon out — it listens for Slack webhooks and replies in-thread.”", "script": "A spec in, a long-running daemon out — it listens for Slack webhooks and replies in-thread. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the credentials", "cue": "[needs-key] Sources demos/.env for the boot beats — Anthropic plus the two Slack secrets.", "script": "Sources demos/.env for the boot beats — Anthropic plus the two Slack secrets. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Operator-supplied demos/.env; the Slack beats also want SLACK_BOT_TOKEN + SLACK_SIGNING_SECRET.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env; the Slack beats also want SLACK_BOT_TOKEN + SLACK_SIGNING_SECRET" } }
      ]
    },
    {
      "title": "Beat 1 · The spec: a Slack bot in six blocks",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“Two lines: what it's called, and target: channel — the long-running daemon shape.”", "script": "Two lines: what it's called, and target: channel — the long-running daemon shape. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "The agent — model and instructions", "cue": "“You are a Slack bot… reply concisely (1-2 sentences) in the same thread.”", "script": "You are a Slack bot… reply concisely (1-2 sentences) in the same thread. [[pause:900ms]] Watch lines 3 through 9 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 9] },
        { "id": "spec-tools", "label": "Give it hands", "cue": "Two hands: read the repo, run a shell. The gate on bash arrives later in the same file.", "script": "Two hands: read the repo, run a shell. The gate on bash arrives later in the same file. [[pause:900ms]] Watch lines 10 through 12 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [10, 12] },
        { "id": "spec-channels", "label": "Wire it to Slack", "cue": "Slack is two env refs. No token ever lands in the spec — the compiler emits the lookup.", "script": "Slack is two env refs. No token ever lands in the spec — the compiler emits the lookup. [[pause:900ms]] Watch lines 13 through 16 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [13, 16] },
        { "id": "spec-routing", "label": "Route a thread to a session", "cue": "“sessionKey: thread — every Slack thread gets its own persistent session.”", "script": "SessionKey: thread — every Slack thread gets its own persistent session. [[pause:900ms]] Watch lines 17 through 18 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [17, 18] },
        { "id": "spec-permissions", "label": "Fence the shell", "cue": "Read auto-allowed, Bash(**) ask-gated — the policy ships in the spec, not in a review.", "script": "Read auto-allowed, Bash(**) ask-gated — the policy ships in the spec, not in a review. [[pause:900ms]] Watch lines 19 through 24 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [19, 24] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The spec is sugar; this JSON is the channel IR the compiler actually sees.”", "script": "The spec is sugar; this JSON is the channel IR the compiler actually sees. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target channel (slack adapter, secret refs unresolved).", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target channel (slack adapter, secret refs unresolved)" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no token in the room.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, with no token in the room. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (4 anchors); install ok; boot gated at the spec env refs gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated at the spec env refs gate", "produces": ["live/dist/daemon.ts", "live/dist/gateway.ts", "live/dist/session-router.ts", "live/dist/agent.ts"] } },
        { "id": "prove-open-gateway", "label": "Open the emitted gateway", "cue": "One spec, five files. The gateway verifies the webhook signature before anything runs.", "script": "One spec, five files. The gateway verifies the webhook signature before anything runs. [[pause:1.2s]] The editor opens live slash dist slash gateway.ts now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/dist/gateway.ts" },
        { "id": "prove-open-router", "label": "Open the session router", "cue": "sha256 of adapter:workspace:channel:thread → sess_<16hex>. Same thread, same session.", "script": "Sha256 of adapter:workspace:channel:thread → sess_<16hex>. Same thread, same session. [[pause:1.2s]] The editor opens live slash dist slash session router.ts now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/dist/session-router.ts" }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL. A daemon isn't a chat — the CLI names the shapes it takes.”", "script": "Run is the cli/browser REPL. A daemon isn't a chat — the CLI names the shapes it takes. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection — `crewhaus run supports target: cli or browser (got \"channel\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — `crewhaus run supports target: cli or browser (got \"channel\")`" } },
        { "id": "run-gate", "label": "Boot the daemon with no secrets", "cue": "“Strip the four secrets back out, and it names every env var it needs before it opens a socket — exit 2.”", "script": "Strip the four secrets back out, and it names every env var it needs before it opens a socket — exit 2. [[pause:1.5s]] The refusal is the proof in this beat, so the verifier expects exit code 2. Designed refusal `[daemon] missing required env vars: SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, ANTHROPIC_AUTH_TOKEN or ANTHROPIC_API_KEY`; deps installed by the --check beat.", "action": "command", "command": "env -u SLACK_BOT_TOKEN -u SLACK_SIGNING_SECRET -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_API_KEY bun live/dist/daemon.ts", "verify": { "mode": "offline", "expectedExit": 2, "note": "designed refusal — `[daemon] missing required env vars: SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, ANTHROPIC_AUTH_TOKEN or ANTHROPIC_API_KEY`; deps installed by the --check beat. `env -u` makes it deterministic: the Setup group already sourced .env into this same terminal, and with those four present the daemon would boot and listen instead" } },
        { "id": "run-tunnel", "label": "Give the webhook a public URL", "cue": "“Use ngrok or similar for local dev” — the Request URL is that host + /slack/events. Run it in your own terminal, not this one: this terminal has beats left to drive.", "script": "“Use ngrok or similar for local dev” — the Request URL is that host + /slack/events. Run it in your own terminal, not this one: this terminal has beats left to drive. [[pause:1.2s]] The editor opens README.md now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "README.md" },
        { "id": "run-boot", "label": "Boot it for real", "cue": "[needs-key] “Secrets present, it listens on PORT — 3000 by default.” Backgrounded, so this same terminal can keep going.", "script": "“Secrets present, it listens on PORT — 3000 by default.” Backgrounded, so this same terminal can keep going. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Needs SLACK_BOT_TOKEN + SLACK_SIGNING_SECRET + Anthropic credentials; long-running, so it runs as job %1 the way starters/managed boots its gateway.", "action": "command", "command": "bun live/dist/daemon.ts &", "verify": { "mode": "needs-key", "note": "needs SLACK_BOT_TOKEN + SLACK_SIGNING_SECRET + Anthropic credentials; long-running, so it runs as job %1 the way starters/managed boots its gateway" } },
        { "id": "run-session", "label": "Count the threads it remembers", "cue": "[manual] @-mention it in a thread, reply, then look: one session file per thread, sess_<16hex>.", "script": "@-mention it in a thread, reply, then look: one session file per thread, sess_<16hex>. [[pause:1.8s]] This step hands control to the operator for the interactive portion. A real Slack workspace has to send the message first; sessions only exist after a live turn. The daemon is backgrounded, so this prompt is free — its log lines interleave.", "action": "command", "command": "ls .crewhaus/sessions | tail -3", "verify": { "mode": "manual", "note": "a real Slack workspace has to send the message first; sessions only exist after a live turn. The daemon is backgrounded, so this prompt is free — its log lines interleave" } },
        { "id": "run-stop", "label": "Stop the daemon", "cue": "[needs-key] Take's over — kill the backgrounded daemon before the last beats.", "script": "Take's over — kill the backgrounded daemon before the last beats. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Job %1 is the daemon the run-boot beat backgrounded; a fresh shell has no such job.", "action": "command", "command": "kill %1", "verify": { "mode": "needs-key", "note": "job %1 is the daemon the run-boot beat backgrounded; a fresh shell has no such job" } }
      ]
    },
    {
      "title": "One more thing · the Slack app setup is a command too",
      "beats": [
        { "id": "extra-provision", "label": "Generate the Slack app manifest", "cue": "“channel provision derives the manifest and the install steps — dry-run prints them.”", "script": "Channel provision derives the manifest and the install steps — dry-run prints them. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. Dry run prints the app manifest YAML + the 4 install steps; writes nothing.", "action": "command", "command": "crewhaus channel provision live/crewhaus.yaml --base-url https://demo.example.com --dry-run", "verify": { "mode": "offline", "expectedExit": 0, "note": "dry run prints the app manifest YAML + the 4 install steps; writes nothing" } },
        { "id": "extra-verify-dry", "label": "Preview the scope doctor", "cue": "“Exactly which call it would make, and which scopes it would compare — before you own an app.”", "script": "Exactly which call it would make, and which scopes it would compare — before you own an app. [[pause:1.5s]] This is an offline proof and should complete successfully. Prints the would-POST auth.test plan; no network.", "action": "command", "command": "crewhaus channel verify live/crewhaus.yaml --dry-run", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the would-POST auth.test plan; no network" } },
        { "id": "extra-verify-live", "label": "Run the scope doctor for real", "cue": "[needs-key] Live auth.test plus the granted scopes — exit 1 the moment one is missing.", "script": "Live auth.test plus the granted scopes — exit 1 the moment one is missing. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Hits slack.com with $SLACK_BOT_TOKEN; exits 1 on missing scopes.", "action": "command", "command": "crewhaus channel verify live/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "hits slack.com with $SLACK_BOT_TOKEN; exits 1 on missing scopes" } }
      ]
    }
  ]
}
