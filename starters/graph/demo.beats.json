{
  "title": "hello-graph — three nodes, two edges, one human gate (target: graph)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "A 3-node graph — plan → execute → summarise — with explicit edges and state persisted per execution.", "script": "A 3-node graph — plan → execute → summarise — with explicit edges and state persisted per execution. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the boot beats.", "script": "Sources demos/.env for the boot beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The spec: three nodes, two edges",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape, pick the entry", "cue": "“Four lines: the name, the graph spine, the model — and which node the run starts at.”", "script": "Four lines: the name, the graph spine, the model — and which node the run starts at. [[pause:900ms]] Watch the driver type lines 1 through 4 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 4], "replace": true },
        { "id": "spec-plan", "label": "Node 1 · plan", "cue": "“Read the user's input from the Upstream state and produce a 3-bullet plan.” No preamble.", "script": "“Read the user's input from the Upstream state and produce a 3-bullet plan.” No preamble. [[pause:900ms]] Watch lines 5 through 10 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [5, 10] },
        { "id": "spec-execute", "label": "Node 2 · execute", "cue": "“Read the plan from the Upstream state's plan field and execute it.” Grounded, 4–6 sentences.", "script": "“Read the plan from the Upstream state's plan field and execute it.” Grounded, 4–6 sentences. [[pause:900ms]] Watch lines 11 through 16 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [11, 16] },
        { "id": "spec-hitl", "label": "Put a human in the loop", "cue": "One block buys an approval gate: the run stops at execute and waits for a person before summarise. The comment says what the gate really does — the checkpoint holds the state from *before* the node, so approving replays it.", "script": "One block buys an approval gate: the run stops at execute and waits for a person before summarise. The comment says what the gate really does — the checkpoint holds the state from *before* the node, so approving replays it. [[pause:900ms]] Watch lines 17 through 22 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [17, 22] },
        { "id": "spec-summarise", "label": "Node 3 · summarise", "cue": "“A 2-sentence executive summary. Do not include lists or bullets — prose only.”", "script": "A 2-sentence executive summary. Do not include lists or bullets — prose only. [[pause:900ms]] Watch lines 23 through 27 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [23, 27] },
        { "id": "spec-edges", "label": "Wire the DAG", "cue": "Nodes are inert until you connect them: plan → execute → summarise.", "script": "Nodes are inert until you connect them: plan → execute → summarise. [[pause:900ms]] Watch lines 28 through 32 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [28, 32] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Graph well-formedness passes the compile path skips, plus a scope audit — nothing emitted.”", "script": "Graph well-formedness passes the compile path skips, plus a scope audit — nothing emitted. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“Nodes, edges and the hitl prompt, lowered to JSON — this is what the compiler actually sees.”", "script": "Nodes, edges and the hitl prompt, lowered to JSON — this is what the compiler actually sees. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target graph (3 nodes, 2 edges, hitlPrompt on execute); the edges carry from/to only — no schema, so they are untyped.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target graph (3 nodes, 2 edges, hitlPrompt on execute); the edges carry from/to only — no schema, so they are untyped" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok; install ok; boot gated at the credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok; install ok; boot gated at the credentials gate", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted graph", "cue": "“addNode, addEdge, setEntry over a checkpoint store — and look inside execute: requestApproval sits after the model turn. The gate is after the work.”", "script": "AddNode, addEdge, setEntry over a checkpoint store — and look inside execute: requestApproval sits after the model turn. The gate is after the work. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" },
        { "id": "prove-worker", "label": "Try to ship it to the edge", "cue": "“Refused: the human gate needs a host. The compiler won't quietly drop it to fit the edge.”", "script": "Refused: the human gate needs a host. The compiler won't quietly drop it to fit the edge. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection — `cf-worker-graph does not support HITL in M2 (node \"execute\" has hitlPrompt)`.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-as cf-worker -o live/worker", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — `cf-worker-graph does not support HITL in M2 (node \"execute\" has hitlPrompt)`" } }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL. A graph is compile-only — the CLI names the shapes it takes.”", "script": "Run is the cli/browser REPL. A graph is compile-only — the CLI names the shapes it takes. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection — `crewhaus run supports target: cli or browser (got \"graph\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — `crewhaus run supports target: cli or browser (got \"graph\")`" } },
        { "id": "run-bundle", "label": "Boot the graph on a real question", "cue": "[needs-key] “Input on stdin: plan and execute both run, then it stops at the gate and prints its own resume line.”", "script": "Input on stdin: plan and execute both run, then it stops at the gate and prints its own resume line. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Target graph has no REPL the emitted bundle reads its input from stdin.", "action": "command", "command": "echo 'Draft a launch announcement for the CrewHaus graph starter.' | bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "target graph has no REPL — the emitted bundle reads its input from stdin. Two model calls: the hitl gate is requestApproval AFTER the execute node's turn, and the checkpoint stores the state at the pause, so execute's own output is not committed" } }
      ]
    },
    {
      "title": "One more thing · the pause is a checkpoint",
      "beats": [
        { "id": "extra-checkpoints", "label": "Look at the run on disk", "cue": "[needs-key] Every run is a directory of checkpoints — state you can resume from or branch off.", "script": "Every run is a directory of checkpoints — state you can resume from or branch off. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. The run dir (grun_…) only exists after the keyed run above; the store is .crewhaus/graphs.", "action": "command", "command": "ls -t .crewhaus/graphs", "verify": { "mode": "needs-key", "note": "the run dir (grun_…) only exists after the keyed run above; the store is .crewhaus/graphs" } },
        { "id": "extra-resume", "label": "Approve, and let it finish", "cue": "[needs-key] Approve the newest run. The checkpoint holds the state at the pause, so execute runs again — this time straight through — and then summarise.", "script": "Approve the newest run. The checkpoint holds the state at the pause, so execute runs again — this time straight through — and then summarise. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Resumes from the head checkpoint with decision `approve`; ls -t picks the newest graphRunId. The paused node never committed its output, so execute re-runs (a second execute model call) with ctx.approval pre-set, then summarise.", "action": "command", "command": "bun live/dist/agent.ts --resume \"$(ls -t .crewhaus/graphs | head -1)\" approve", "verify": { "mode": "needs-key", "note": "resumes from the head checkpoint with decision `approve`; ls -t picks the newest graphRunId. The paused node never committed its output, so execute re-runs (a second execute model call) with ctx.approval pre-set, then summarise" } }
      ]
    }
  ]
}
