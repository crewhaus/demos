{
  "title": "hello-eval — a math agent, a labelled split, a grader (target: eval)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“A math agent, a labelled dev split, an exact_match grader.” That's the whole shape.", "script": "“A math agent, a labelled dev split, an exact_match grader.” That's the whole shape. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — scoring samples means really calling the model.", "script": "Sources demos/.env — scoring samples means really calling the model. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · Thirteen lines of spec",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“target: eval — this spec compiles to a scorer, not a chat loop.”", "script": "Target: eval — this spec compiles to a scorer, not a chat loop. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "Add the agent under test", "cue": "“The thing being measured: answer math questions with just the number.”", "script": "The thing being measured: answer math questions with just the number. [[pause:900ms]] Watch lines 3 through 6 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 6] },
        { "id": "spec-dataset", "label": "Point at the dataset", "cue": "“Not a file path — a registry reference: name, version, split.”", "script": "Not a file path — a registry reference: name, version, split. [[pause:900ms]] Watch lines 7 through 10 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [7, 10] },
        { "id": "spec-graders", "label": "Add the grader", "cue": "“exact_match — deterministic. No judge, no rubric, nothing to argue with.”", "script": "Exact_match — deterministic. No judge, no rubric, nothing to argue with. [[pause:900ms]] Watch lines 11 through 12 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [11, 12] },
        { "id": "spec-open-graders", "label": "Open the standalone graders config", "cue": "The same grader by type — this is the file the CLI eval subcommand reads.", "script": "The same grader by type — this is the file the CLI eval subcommand reads. [[pause:1.2s]] The editor opens graders.yaml now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "graders.yaml" },
        { "id": "spec-concurrency", "label": "Set the fan-out", "cue": "“Two samples in flight at a time.”", "script": "The evaluation runner is deliberately bounded to two samples in flight, making parallel scoring explicit and predictable. [[pause:900ms]] Watch lines 13 through 13 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [13, 13] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-datasets", "label": "Resolve the registry reference", "cue": "“The dataset is vendored into .crewhaus/datasets — train, dev and test, right here.”", "script": "The dataset is vendored into .crewhaus/datasets — train, dev and test, right here. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints hello-eval v1 with its per-split sample counts.", "action": "command", "command": "crewhaus datasets list", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints hello-eval v1 with its per-split sample counts" } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The spec is sugar; this JSON is what the compiler actually sees — dataset ref, grader, fan-out.”", "script": "The spec is sugar; this JSON is what the compiler actually sees — dataset ref, grader, fan-out. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the whole IR for target eval (21 lines) — dataset ref, graders, concurrency; the emitted bundle rebuilds its own agent IR and passes these three as separate runEval arguments.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the whole IR for target eval (21 lines) — dataset ref, graders, concurrency; the emitted bundle rebuilds its own agent IR and passes these three as separate runEval arguments" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the emitted shape, installs its deps, and boots it: GREEN, no key.”", "script": "--check asserts the emitted shape, installs its deps, and boots it: GREEN, no key. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (3 anchors); install ok; boot gated at the registered-dataset gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (3 anchors); install ok; boot gated at the registered-dataset gate", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted scorer", "cue": "“It imports runEval, reads the registry, applies the graders, prints the score.”", "script": "It imports runEval, reads the registry, applies the graders, prints the score. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Score the dev split",
      "beats": [
        { "id": "run-bundle", "label": "Run the compiled scorer", "cue": "[needs-key] “The bundle drives the real runEval” — runId, passRate, samples, report dir.", "script": "“The bundle drives the real runEval” — runId, passRate, samples, report dir. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Without credentials every sample fails and the run reports passRate: 0.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "without credentials every sample fails and the run reports passRate: 0" } },
        { "id": "run-open-dataset", "label": "Open the flat dev split", "cue": "input and expected_output, one JSON object per line — what --dataset takes.", "script": "Input and expected_output, one JSON object per line — what --dataset takes. [[pause:1.2s]] The editor opens .crewhaus slash datasets slash hello eval slash dev.jsonl now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": ".crewhaus/datasets/hello-eval/dev.jsonl" },
        { "id": "run-open-cli-spec", "label": "Open the cli companion spec", "cue": "“The same math agent as a target: cli shape” — the one the eval subcommand drives.", "script": "“The same math agent as a target: cli shape” — the one the eval subcommand drives. [[pause:1.2s]] The editor opens agent.cli.yaml now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "agent.cli.yaml" },
        { "id": "run-cli-eval", "label": "Score it through the CLI", "cue": "[needs-key] Same agent, same data, same grader — no bundle in the loop.", "script": "Same agent, same data, same grader — no bundle in the loop. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Writes grades + an HTML report under live/eval.", "action": "command", "command": "crewhaus eval agent.cli.yaml --dataset .crewhaus/datasets/hello-eval/dev.jsonl --graders graders.yaml -o live/eval", "verify": { "mode": "needs-key", "note": "writes grades + an HTML report under live/eval" } }
      ]
    },
    {
      "title": "One more thing · an eval is a gate, not a number",
      "beats": [
        { "id": "extra-gate", "label": "Run it as a gate", "cue": "[needs-key] “--gate exits non-zero on regression vs the pinned baseline.” That's the CI job.", "script": "“--gate exits non-zero on regression vs the pinned baseline.” That's the CI job. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Diffs against the pin run-cli-eval just set (same spec name + dataset = same baseline key): regressions=0 → “gate: PASS” → the new run is promoted; a regression exits non-zero instead.", "action": "command", "command": "crewhaus eval agent.cli.yaml --dataset .crewhaus/datasets/hello-eval/dev.jsonl --graders graders.yaml --gate -o live/eval-gate", "verify": { "mode": "needs-key", "note": "diffs against the pin run-cli-eval just set (same spec name + dataset = same baseline key): regressions=0 → “gate: PASS” → the new run is promoted; a regression exits non-zero instead" } },
        { "id": "extra-history", "label": "List the recorded runs", "cue": "Every `crewhaus eval` run lands in .crewhaus/evals/index.jsonl — a score history, not a one-off number. The compiled bundle isn't in the table: it writes its own run dir and nothing else.", "script": "Every `crewhaus eval` run lands in .crewhaus/evals/index.jsonl — a score history, not a one-off number. The compiled bundle isn't in the table: it writes its own run dir and nothing else. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. On camera: the two CLI runs above, the gate run flagged as base. Only `crewhaus eval` appends the index — offline verification runs this before any scored run, so it prints “no recorded runs match”.", "action": "command", "command": "crewhaus eval-report history", "verify": { "mode": "offline", "expectedExit": 0, "note": "on camera: the two CLI runs above, the gate run flagged as base. Only `crewhaus eval` appends the index — offline verification runs this before any scored run, so it prints “no recorded runs match”" } },
        { "id": "extra-baseline", "label": "Show the pinned baseline", "cue": "And the pin every later run is measured against — keyed by spec and dataset.", "script": "And the pin every later run is measured against — keyed by spec and dataset. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Empty before the first scored run: “no baselines pinned”.", "action": "command", "command": "crewhaus eval-report baseline show", "verify": { "mode": "offline", "expectedExit": 0, "note": "empty before the first scored run: “no baselines pinned”" } }
      ]
    }
  ]
}
