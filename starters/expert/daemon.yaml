# hello-expert (daemon) — the SAME self-teaching expert as crewhaus.yaml,
# compiled to an always-on Slack daemon. Two things change versus the cli:
#
#   1. heartbeat + `learning.study.on_heartbeat` — every 6h a tick fires and
#      the daemon runs an unattended study pass. The rotation policy (gaps
#      first, ~3 STUDY : 1 REFLECT, bounded per tick) ships with CrewHaus
#      and is baked ahead of the operator instructions below at compile time
#      — HEARTBEAT.md documents it, nothing re-implements it.
#   2. channel — it answers questions in Slack threads, and 👍/👎 reactions
#      on its replies become rating signal for the flywheel.
#
# THE SECOND KEY — the reason this file is worth reading. The daemon does NOT
# reuse the cli spec's THREDZ_API_KEY. It carries its own $THREDZ_DAEMON_KEY
# and its own `space: coffee-daemon`, because two facts meet here:
#
#   · Thredz allows ONE individual wiki space per API KEY — asking a key for a
#     second one comes back 409 individual_space_exists.
#   · thredz-mcp reads ONE THREDZ_API_KEY per PROCESS, and each spec compiles
#     to its own process with its own synthesized server.
#
# So per-agent private memory = one key per agent = one server process per
# agent. Everything this daemon studies unattended lands in `coffee-daemon`,
# which the cli expert's key cannot read. Want ONE brain across both shapes
# instead? Create a SHARED space and put its slug in both specs — a shared
# space is readable by every wiki-enabled key on the account.
#
# Going hosted trades away one thing worth knowing: on the LOCAL wiki backend
# `wiki_write` DETERMINISTICALLY rejects an uncited body (the `## Sources`
# gate is enforced in code), whereas on Thredz that same discipline is the
# learning-loop skill's standing instruction. Delete the `thredz:` block below
# and the `memory.wiki` block falls back to local files — that is the
# mechanical rejection, live.
#
# Usage (from inside this directory) — `crewhaus run` is cli/browser-only and
# will NOT serve a channel daemon, so compile and run the bundle (or use the
# `crewhaus dev` supervised loop, which works here because this spec has no
# local-relative-path MCP servers):
#   cp .env.example .env   # ANTHROPIC_API_KEY + THREDZ_DAEMON_KEY + SLACK_*
#   crewhaus dev daemon.yaml                       # supervised dev loop
#   # — or, standalone:
#   bunx crewhaus compile daemon.yaml -o dist && bun install --cwd dist && bun dist/daemon.ts
# For a fast local heartbeat while testing, drop `heartbeat.every` to "5m".

name: hello-expert-daemon
target: channel

memory:
  wiki: { enabled: true, autoRecall: true } # autoRecall folds the recalled slice into every session start; the block below flips the backend from local files to Thredz

# A SECOND key, deliberately — see the header. One individual space per API
# key is a hard Thredz limit, so the daemon needs its own key to get memory
# the interactive expert cannot read.
thredz:
  api_key: $THREDZ_DAEMON_KEY
  space: coffee-daemon

learning:
  domain: specialty coffee brewing & extraction science
  curriculum: curriculum.md
  sources: ["sca.coffee", "*.edu", "baristahustle.com"]
  # study.on_heartbeat defaults ON: every heartbeat tick doubles as a
  # study/reflect tick per the built-in rotation.

agent:
  model: claude-sonnet-5
  model_fallbacks: [claude-haiku-4-5-20251001]
  instructions: |
    You are the resident specialty-coffee expert answering in Slack threads.
    Keep answers to 1-3 sentences unless asked for depth, and reply
    in-thread. Precision over fluency: grind size, brew ratios, extraction
    yields, TDS, and water chemistry are load-bearing numbers — get them
    right, with units, or say you are unsure. Prefer SCA standards and
    peer-reviewed extraction science; treat blogs and forums as leads to
    verify, not sources. Local field notes live in sources/.
  sub_agents:
    researcher:
      description: |
        Read-only source researcher. Give it ONE narrow sub-question. It
        searches/fetches high-quality sources and returns 2-4 verbatim,
        cited snippets — no synthesis. Hard cap: 180 words.
      instructions: |
        You research ONE narrow question against high-quality sources.
        Prefer primary literature, standards bodies, and canonical texts.
        Return 2-4 SHORT verbatim snippets, each with its source, and one
        line on time-tested vs frontier. Do NOT synthesise. Under 180 words.
      tools: [webSearch, webFetch, read]
      model: claude-haiku-4-5-20251001
      permissions:
        allow: [WebSearch, WebFetch, Read]
        deny: []
  tools: [read, edit, webSearch, webFetch, todoWrite]

# One inbound message = one agent turn. `sessionKey: user` gives each person
# their own continuing session — and, crucially, lets 👍/👎 reactions become
# rating signal: reaction feedback only lands under `channel`/`user` keys,
# NOT `thread` (a reaction carries the bot reply's ts, not the thread root,
# so a thread-keyed session can't be recovered from it — see recipe 62).
# Replies still post in the triggering thread regardless of the session key.
channels:
  slack:
    botToken: $SLACK_BOT_TOKEN
    signingSecret: $SLACK_SIGNING_SECRET
routing:
  sessionKey: user

# The scheduled learning cadence. The built-in study rotation is prepended
# to these instructions at compile time (learning.study.on_heartbeat).
heartbeat:
  every: 6h
  instructions: |
    Do the pass the rotation selects, then log a one-line result. If there
    is genuinely nothing useful to do this tick, log `heartbeat: idle`.

# 👍/👎 on the bot's replies become rating signal for the flywheel (now that
# sessionKey is `user`, above). NOTE: `autoDistill` runs at cli-run teardown
# and is inert on a channel daemon — to fold these ratings into a dataset,
# run `crewhaus distill --all-sessions --register hello-expert-ratings` from
# this directory on a schedule (or in the flywheel workflow).
feedback:
  channelReactions: true

# A tiny status page for the daemon (health, turn counts, heartbeat ticks).
gateway:
  port: 4173
  ui: true

permissions:
  mode: default
  rules:
    - { type: alwaysAllow, pattern: Read }
    - { type: alwaysAllow, pattern: WebSearch }
    - { type: alwaysAllow, pattern: WebFetch }
    - { type: alwaysAllow, pattern: TodoWrite }
    - { type: alwaysAllow, pattern: wiki_recall }
    - { type: alwaysAllow, pattern: wiki_semantic_search }
    - { type: alwaysAllow, pattern: wiki_search }
    - { type: alwaysAllow, pattern: wiki_get }
    - { type: alwaysAllow, pattern: wiki_list }
    - { type: alwaysAllow, pattern: wiki_related }
    - { type: alwaysAllow, pattern: wiki_stats }
    - { type: alwaysAllow, pattern: wiki_space_list } # "which spaces can this key see?"
    - { type: alwaysAllow, pattern: wiki_write }
    - { type: alwaysAllow, pattern: wiki_set_signals }
    - { type: alwaysAllow, pattern: log_knowledge_gap }
    - { type: alwaysAllow, pattern: task_list } # gaps arrive as Thredz tasks on this backend — the tick lists them first
    - { type: alwaysAllow, pattern: Edit(curriculum.md) }
    - { type: alwaysAllow, pattern: Edit(eval/**) }
    - { type: alwaysAllow, pattern: Edit(sources/**) }
