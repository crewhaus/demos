{
  "title": "hello-research — an autonomous research agent that must cite (target: research)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“An agent that researches one sub-question at a time using local source documents.”", "script": "An agent that researches one sub-question at a time using local source documents. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the run beats.", "script": "Sources demos/.env for the run beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The spec — a research contract",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“target: research” — plan, branch, cite, report. No REPL in this one.", "script": "“target: research” — plan, branch, cite, report. No REPL in this one. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "Add the agent and its job", "cue": "Haiku, and the rule that shapes everything after it: one sub-question at a time.", "script": "Haiku, and the rule that shapes everything after it: one sub-question at a time. [[pause:900ms]] Watch lines 3 through 7 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 7] },
        { "id": "spec-workflow", "label": "Spell out the four steps", "cue": "Load every source, pick at most 2 verbatim snippets, CiteFact each one, then answer.", "script": "Load every source, pick at most 2 verbatim snippets, CiteFact each one, then answer. [[pause:900ms]] Watch lines 8 through 20 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [8, 20] },
        { "id": "spec-goal", "label": "Give it the goal", "cue": "The question ships in the spec: what shapes exist, grouped shipped versus planned.", "script": "The question ships in the spec: what shapes exist, grouped shipped versus planned. [[pause:900ms]] Watch lines 21 through 24 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [21, 24] },
        { "id": "spec-budget", "label": "Cap the run", "cue": "Three branches, four minutes. The budget is a spec field, not a hope.", "script": "Three branches, four minutes. The budget is a spec field, not a hope. [[pause:900ms]] Watch lines 25 through 26 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [25, 26] },
        { "id": "spec-fence", "label": "Fence the filesystem", "cue": "Its entire world is ./sources — harness-relative, so a copy works anywhere.", "script": "Its entire world is ./sources — harness-relative, so a copy works anywhere. [[pause:900ms]] Watch lines 27 through 33 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [27, 33] },
        { "id": "spec-open-sources", "label": "Open the corpus it's fenced to", "cue": "Three markdown files. This is the one with the shipped-versus-planned split.", "script": "Three markdown files. This is the one with the shipped-versus-planned split. [[pause:1.2s]] The editor opens sources slash target shapes roadmap.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "sources/target-shapes-roadmap.md" }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "Goal, branching factor and the allowed file roots all lower into the IR.", "script": "Goal, branching factor and the allowed file roots all lower into the IR. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target research (33 lines).", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target research (33 lines)" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (7 anchors); install ok; boot gated at the credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (7 anchors); install ok; boot gated at the credentials gate", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "One .ts file — the other is the generated README — and it holds the whole loop: decompose the goal, run a branch each, write a cited report.", "script": "One .ts file — the other is the generated README — and it holds the whole loop: decompose the goal, run a branch each, write a cited report. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-refuse", "label": "Try crewhaus run", "cue": "“run supports target cli or browser” — research is compile-only. The bundle is the runtime.", "script": "“run supports target cli or browser” — research is compile-only. The bundle is the runtime. [[pause:1.5s]] The refusal is the proof in this beat, so the verifier expects exit code 1. Deliberate rejection, exit 1: `crewhaus run` supports cli|browser; other shapes are compile-only.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection, exit 1: `crewhaus run` supports cli|browser; other shapes are compile-only" } },
        { "id": "run-bundle", "label": "Boot the bundle", "cue": "[needs-key] It writes its own sub-questions first, then researches them one branch at a time.", "script": "It writes its own sub-questions first, then researches them one branch at a time. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Plan → branch per sub-question → report; needs Anthropic credentials.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "plan → branch per sub-question → report; needs Anthropic credentials" } },
        { "id": "run-report", "label": "Read the report it wrote", "cue": "[needs-key] No merge step, no executive summary: the goal as the title, one section per sub-question, then the numbered Citations list — each number a file:// URI and the snippet a branch quoted from it.", "script": "No merge step, no executive summary: the goal as the title, one section per sub-question, then the numbered Citations list — each number a file:// URI and the snippet a branch quoted from it. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Newest report only a bare `*/report.md` glob cats every earlier run on a re-take.", "action": "command", "command": "cat \"$(ls -t .crewhaus/research/*/report.md | head -1)\"", "verify": { "mode": "needs-key", "note": "newest report only — a bare `*/report.md` glob cats every earlier run on a re-take. report-writer's renderMarkdown emits exactly `# <goal>`, `## <sub-question>` + that branch's answer, then `## Citations`; there is no synthesis pass, and the Citations block appears only if some branch actually called CiteFact" } }
      ]
    },
    {
      "title": "One more thing · the goal is a flag",
      "beats": [
        { "id": "extra-goal", "label": "Point it at a different question", "cue": "[needs-key] Same bundle, same fenced corpus — a new goal and a single branch.", "script": "Same bundle, same fenced corpus — a new goal and a single branch. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. --goal and --branching override the spec defaults at launch.", "action": "command", "command": "bun live/dist/agent.ts --goal \"Which target shapes are still planned, and what ships each one?\" --branching 1", "verify": { "mode": "needs-key", "note": "--goal and --branching override the spec defaults at launch" } },
        { "id": "extra-runs", "label": "List the runs on disk", "cue": "[needs-key] One directory per run: report.md, report.json, and the state.json --resume reads.", "script": "One directory per run: report.md, report.json, and the state.json --resume reads. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. The directory only exists after a needs-key run.", "action": "command", "command": "ls .crewhaus/research", "verify": { "mode": "needs-key", "note": "the directory only exists after a needs-key run" } }
      ]
    }
  ]
}
