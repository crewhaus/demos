{
  "title": "hello-channel-discord — the same shape, a different platform (target: channel)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“Serves Discord's interactions endpoint at POST /discord/events — one model turn per slash command, button click, or modal submit.” Signed HTTP only: no Gateway WebSocket, and registering the commands is a call provision prints for you.", "script": "“Serves Discord's interactions endpoint at POST /discord/events — one model turn per slash command, button click, or modal submit.” Signed HTTP only: no Gateway WebSocket, and registering the commands is a call provision prints for you. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the credentials", "cue": "[needs-key] Sources demos/.env for the boot beats — Anthropic plus the three Discord refs.", "script": "Sources demos/.env for the boot beats — Anthropic plus the three Discord refs. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Operator-supplied demos/.env; the Discord beats also want DISCORD_APPLICATION_ID + DISCORD_BOT_TOKEN + DISCORD_PUBLIC_KEY.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env; the Discord beats also want DISCORD_APPLICATION_ID + DISCORD_BOT_TOKEN + DISCORD_PUBLIC_KEY" } }
      ]
    },
    {
      "title": "Beat 1 · The spec: swap the platform, keep the shape",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“Same target: channel as the Slack starter. The platform is a block, not a rewrite.”", "script": "Same target: channel as the Slack starter. The platform is a block, not a rewrite. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "The agent — model and instructions", "cue": "“When a slash command, button click, or modal submit fires, reply concisely.”", "script": "When a slash command, button click, or modal submit fires, reply concisely. [[pause:900ms]] Watch lines 3 through 8 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 8] },
        { "id": "spec-tools", "label": "Give it one hand", "cue": "Read only — no shell here, so there is nothing to ask about at runtime.", "script": "Read only — no shell here, so there is nothing to ask about at runtime. [[pause:900ms]] Watch lines 9 through 10 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [9, 10] },
        { "id": "spec-channels", "label": "Wire it to Discord", "cue": "Three env refs: application id, bot token, and the public key that verifies each interaction.", "script": "Three env refs: application id, bot token, and the public key that verifies each interaction. [[pause:900ms]] Watch lines 11 through 15 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [11, 15] },
        { "id": "spec-routing", "label": "Route an interaction to a session", "cue": "The same sessionKey: thread as the Slack starter — but read it in Discord's terms: “thread” is a real thread channel, and a slash command in a plain channel is keyed by its own interaction id, so each one opens a fresh session.", "script": "The same sessionKey: thread as the Slack starter — but read it in Discord's terms: “thread” is a real thread channel, and a slash command in a plain channel is keyed by its own interaction id, so each one opens a fresh session. [[pause:900ms]] Watch lines 16 through 17 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [16, 17] },
        { "id": "spec-permissions", "label": "State the policy anyway", "cue": "One rule: Read is always allowed. The permission block is in the spec even when it is short.", "script": "One rule: Read is always allowed. The permission block is in the spec even when it is short. [[pause:900ms]] Watch lines 18 through 21 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [18, 21] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“Same IR target as the Slack starter — only the adapter and its secret refs differ.”", "script": "Same IR target as the Slack starter — only the adapter and its secret refs differ. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target channel (discord adapter, three secret refs).", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target channel (discord adapter, three secret refs)" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no token in the room.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, with no token in the room. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (4 anchors); install ok; boot gated at the spec env refs gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated at the spec env refs gate", "produces": ["live/dist/daemon.ts", "live/dist/gateway.ts", "live/dist/session-router.ts", "live/dist/agent.ts"] } },
        { "id": "prove-open-daemon", "label": "Open the emitted daemon", "cue": "The Discord adapter is one import swap; the gateway, router and agent are the same three files.", "script": "The Discord adapter is one import swap; the gateway, router and agent are the same three files. [[pause:1.2s]] The editor opens live slash dist slash daemon.ts now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/dist/daemon.ts" },
        { "id": "prove-adapter", "label": "Find the platform in the bundle", "cue": "“One import names the platform. Everything else the compiler emits is shape, not Discord.”", "script": "One import names the platform. Everything else the compiler emits is shape, not Discord. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Two hits: channel-adapter-discord (the platform) and channel-adapter-base (the shape).", "action": "command", "command": "grep -n channel-adapter live/dist/daemon.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "two hits: channel-adapter-discord (the platform) and channel-adapter-base (the shape)" } }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL. A daemon isn't a chat — the CLI names the shapes it takes.”", "script": "Run is the cli/browser REPL. A daemon isn't a chat — the CLI names the shapes it takes. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection — `crewhaus run supports target: cli or browser (got \"channel\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — `crewhaus run supports target: cli or browser (got \"channel\")`" } },
        { "id": "run-gate", "label": "Boot the daemon with no secrets", "cue": "“Scrub the secrets back out and it names every one of them — before it binds a port — then exits 2.”", "script": "Scrub the secrets back out and it names every one of them — before it binds a port — then exits 2. [[pause:1.5s]] The refusal is the proof in this beat, so the verifier expects exit code 2. Designed refusal `[daemon] missing required env vars: DISCORD_APPLICATION_ID, DISCORD_BOT_TOKEN, DISCORD_PUBLIC_KEY, ANTHROPIC_AUTH_TOKEN or ANTHROPIC_API_KEY`.", "action": "command", "command": "env -u DISCORD_APPLICATION_ID -u DISCORD_BOT_TOKEN -u DISCORD_PUBLIC_KEY -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_API_KEY bun live/dist/daemon.ts", "verify": { "mode": "offline", "expectedExit": 2, "note": "designed refusal — `[daemon] missing required env vars: DISCORD_APPLICATION_ID, DISCORD_BOT_TOKEN, DISCORD_PUBLIC_KEY, ANTHROPIC_AUTH_TOKEN or ANTHROPIC_API_KEY`. env -u is load-bearing: Setup already sourced .env into this same terminal, so without it a provisioned take boots the daemon instead of showing the gate" } },
        { "id": "run-boot", "label": "Boot it for real", "cue": "[needs-key] “Secrets present, and it serves the interactions endpoint on PORT — 3000 by default.” Backgrounded, so the terminal stays yours.", "script": "“Secrets present, and it serves the interactions endpoint on PORT — 3000 by default.” Backgrounded, so the terminal stays yours. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Needs the three Discord vars + Anthropic credentials; long-running HTTP interactions endpoint.", "action": "command", "command": "bun live/dist/daemon.ts &", "verify": { "mode": "needs-key", "note": "needs the three Discord vars + Anthropic credentials; long-running HTTP interactions endpoint — `&` keeps the manifest's one terminal free for the beats after it (`kill %1` when the take is done, or port 3000 is still held on the re-take)" } },
        { "id": "run-slash", "label": "Fire a slash command", "cue": "[manual] Invoke the command in Discord; the adapter renders it as “/<name> <options>” for the agent.", "script": "Invoke the command in Discord; the adapter renders it as “/<name> <options>” for the agent. [[pause:1.8s]] This is the hands-on portion of the take, so the operator completes it while the driver keeps the context visible. A real Discord app + a public interactions URL has to deliver the interaction first.", "action": "command", "command": "ls .crewhaus/sessions | tail -3", "verify": { "mode": "manual", "note": "a real Discord app + a public interactions URL has to deliver the interaction first" } }
      ]
    },
    {
      "title": "One more thing · the Discord app setup is a command too",
      "beats": [
        { "id": "extra-provision", "label": "Derive the interactions endpoint + invite URL", "cue": "“channel provision derives the endpoint PATCH, the permission bits, and the invite URL.”", "script": "Channel provision derives the endpoint PATCH, the permission bits, and the invite URL. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Dry run prints the applications/@me PATCH payload + invite URL + the slash-command registration step; writes nothing.", "action": "command", "command": "crewhaus channel provision live/crewhaus.yaml --platform discord --base-url https://demo.example.com --dry-run", "verify": { "mode": "offline", "expectedExit": 0, "note": "dry run prints the applications/@me PATCH payload + invite URL + the slash-command registration step; writes nothing" } },
        { "id": "extra-verify-dry", "label": "Preview the endpoint doctor", "cue": "“Exactly one call it would make — GET applications/@me — before you own an app.”", "script": "Exactly one call it would make — GET applications/@me — before you own an app. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. Prints the would-GET applications/@me plan; no network.", "action": "command", "command": "crewhaus channel verify live/crewhaus.yaml --dry-run", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the would-GET applications/@me plan; no network" } },
        { "id": "extra-verify-live", "label": "Run the endpoint doctor for real", "cue": "[needs-key] Live application fetch — exit 1 the moment the interactions URL doesn't match.", "script": "Live application fetch — exit 1 the moment the interactions URL doesn't match. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Hits discord.com with $DISCORD_BOT_TOKEN; exits 1 on a mismatched interactions endpoint.", "action": "command", "command": "crewhaus channel verify live/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "hits discord.com with $DISCORD_BOT_TOKEN; exits 1 on a mismatched interactions endpoint" } }
      ]
    }
  ]
}
