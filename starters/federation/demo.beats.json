{
  "title": "hello-federation — the smoke is the demo (the one starter with no crewhaus.yaml)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“This directory is intentionally light.” The only starter with no spec — and that's the point.", "script": "“This directory is intentionally light.” The only starter with no spec — and that's the point. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — for symmetry with every other drive. Nothing in this one actually needs a key.", "script": "Sources demos/.env — for symmetry with every other drive. Nothing in this one actually needs a key. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env. Nothing below needs a key — the smoke is hermetic and `federation discover` is a deliberate connection failure.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env. Nothing below needs a key — the smoke is hermetic and `federation discover` is a deliberate connection failure." } }
      ]
    },
    {
      "title": "Beat 1 · There is no spec to type",
      "beats": [
        { "id": "spec-none", "label": "Open the smoke instead", "cue": "“No crewhaus.yaml to compile — the runtime lives in the deployments.” Six probes stand in.", "script": "“No crewhaus.yaml to compile — the runtime lives in the deployments.” Six probes stand in. [[pause:1.2s]] The editor opens smoke slash section 34 federation smoke slash smoke.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "../../smoke/section-34-federation-smoke/smoke.ts" },
        { "id": "spec-packages", "label": "Find the three packages", "cue": "Protocol, discovery, router — federation is composed in code. There is no spec.federation block.", "script": "Protocol, discovery, router — federation is composed in code. There is no spec.federation block. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. 3 matching imports — @crewhaus/federation-discovery, -protocol, -router.", "action": "command", "command": "grep -n \"@crewhaus/federation\" ../../smoke/section-34-federation-smoke/smoke.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "3 matching imports — @crewhaus/federation-discovery, -protocol, -router" } }
      ]
    },
    {
      "title": "Beat 2 · Run the six probes",
      "beats": [
        { "id": "prove-smoke", "label": "Run the federation smoke", "cue": "“No docker required, no live cross-host call.” Fourteen checks, end to end, all green.", "script": "“No docker required, no live cross-host call.” Fourteen checks, end to end, all green. [[pause:1.5s]] This is an offline proof and should complete successfully. 14 ✓ checks across probes A–E; probe F is skipped without CREWHAUS_FEDERATION_LIVE=1.", "action": "command", "command": "bun run smoke:section-34", "verify": { "mode": "offline", "expectedExit": 0, "note": "14 ✓ checks across probes A–E; probe F is skipped without CREWHAUS_FEDERATION_LIVE=1. Runs from a clean demos checkout after `bun install`: smoke/section-34-federation-smoke/package.json depends on @crewhaus/federation-{protocol,discovery,router} and vendors probe C's test-only mTLS fixtures (fixtures-cert.pem / fixtures-key.pem) beside smoke.ts. A contributor with a sibling ../factory checkout gets that source instead — the root tsconfig.json `paths` block wins over node_modules. `bun run smoke:section-34` resolves from the root package.json, so it runs fine from this starter dir." } }
      ]
    },
    {
      "title": "Beat 3 · Fail closed",
      "beats": [
        { "id": "run-discover", "label": "Ask the CLI to find a peer", "cue": "Discovery is the CLI's one federation verb — point it at a peer that isn't there and it fails closed.", "script": "Discovery is the CLI's one federation verb — point it at a peer that isn't there and it fails closed. [[pause:1.5s]] The nonzero result is intentional: this command expects exit code 1. Deliberate rejection — deployment-b.example is the smoke's fixture peer, not a real host: `federation discover: peer discovery failed`.", "action": "command", "command": "crewhaus federation discover deployment-b.example --format json", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — deployment-b.example is the smoke's fixture peer, not a real host: `federation discover: peer discovery failed`" } },
        { "id": "run-taxonomy", "label": "Read the error taxonomy", "cue": "ECONNREFUSED retries, a 503 retries — but a cert-pin mismatch tombstones the peer.", "script": "ECONNREFUSED retries, a 503 retries — but a cert-pin mismatch tombstones the peer. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Probe D — 4 assertions over the retry / tombstone / fail taxonomy, plus the import.", "action": "command", "command": "grep -n \"classifyRouterError\" ../../smoke/section-34-federation-smoke/smoke.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "probe D — 4 assertions over the retry / tombstone / fail taxonomy, plus the import" } }
      ]
    },
    {
      "title": "One more thing · how production wires peers",
      "beats": [
        { "id": "extra-wiring", "label": "Show both sides of a federated call", "cue": "The caller builds a router and calls; the callee decodes the envelope and runs the named role.", "script": "The caller builds a router and calls; the callee decodes the envelope and runs the named role. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. 8 matching lines — the exact three calls the README's \"How production wires peers\" names.", "action": "command", "command": "grep -n \"createFederationRouter\\|router.call\\|decodeFederationEnvelope\" ../../smoke/section-34-federation-smoke/smoke.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "8 matching lines — the exact three calls the README's \"How production wires peers\" names" } },
        { "id": "extra-walkthrough", "label": "Open walkthrough 27", "cue": "The envelope shape, mTLS pinning, traceparent propagation, error classification — in full.", "script": "The envelope shape, mTLS pinning, traceparent propagation, error classification — in full. [[pause:1.2s]] The editor opens walkthroughs slash 27 federation.md now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "../../walkthroughs/27-federation.md" }
      ]
    }
  ]
}
