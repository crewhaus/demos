{
  "title": "hello-prochat — a pro-grade multi-modal chat assistant (target: cli)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“Web search, page fetch, image reading, image generation, a sandboxed code interpreter.”", "script": "Web search, page fetch, image reading, image generation, a sandboxed code interpreter. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the run beats.", "script": "Sources demos/.env for the run beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } },
        { "id": "setup-sandbox", "label": "Pre-flight the code interpreter", "cue": "Python, JavaScript and Shell each run in a container — this probes all three images. A ✗ here means start Docker (or Podman) before you roll, not mid-take.", "script": "Python, JavaScript and Shell each run in a container — this probes all three images. A ✗ here means start Docker (or Podman) before you roll, not mid-take. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Probes python:3.13-slim / node:22-alpine / alpine:3.19 through the configured backend (CREWHAUS_SANDBOX, default docker). Exit 0 either way.", "action": "command", "command": "crewhaus sandbox doctor --probe", "verify": { "mode": "offline", "expectedExit": 0, "note": "probes python:3.13-slim / node:22-alpine / alpine:3.19 through the configured backend (CREWHAUS_SANDBOX, default docker). Exit 0 either way — a missing backend shows as ✗ with the reason, which is the point of the beat: the run-math beat needs these green" } },
        { "id": "setup-image-provider", "label": "Pick the image-generation fallback", "cue": "OPENAI_API_KEY wins whenever it's set. This line is what makes /imagine degrade to a placeholder instead of a hard error when it isn't.", "script": "OPENAI_API_KEY wins whenever it's set. This line is what makes /imagine degrade to a placeholder instead of a hard error when it isn't. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. Tool-image-generation resolves OPENAI_API_KEY → openai, else CREWHAUS_IMAGE_PROVIDER=mock → mock, else openai (which then throws “OPENAI_API_KEY is not set”). Without this export there is no fallback.", "action": "command", "command": "export CREWHAUS_IMAGE_PROVIDER=mock", "verify": { "mode": "offline", "expectedExit": 0, "note": "tool-image-generation resolves OPENAI_API_KEY → openai, else CREWHAUS_IMAGE_PROVIDER=mock → mock, else openai (which then throws “OPENAI_API_KEY is not set”). Without this export there is no fallback" } }
      ]
    },
    {
      "title": "Beat 1 · The spec, block by block",
      "beats": [
        { "id": "spec-header", "label": "The header: what it is, what it needs, what to ask it", "cue": "A tier-one chat app in a terminal — and, before the demo prompts, the three things it reaches for outside the model key: a container runtime for the code tools, an OpenAI key for /imagine, and a search provider for WebSearch.", "script": "A tier-one chat app in a terminal — and, before the demo prompts, the three things it reaches for outside the model key: a container runtime for the code tools, an OpenAI key for /imagine, and a search provider for WebSearch. [[pause:900ms]] Watch the driver type lines 1 through 25 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 25], "replace": true },
        { "id": "spec-shape", "label": "Name it, pick the shape", "cue": "Two lines: what it's called, and which runtime spine to compile.", "script": "Two lines: what it's called, and which runtime spine to compile. [[pause:900ms]] Watch lines 26 through 28 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [26, 28] },
        { "id": "spec-capabilities", "label": "The model, and what it can reach", "cue": "Sonnet, then the manifest: search, fetch, see, generate, ingest, execute, delegate — each one stating its own limits, so the model never promises a capability it doesn't have.", "script": "Sonnet, then the manifest: search, fetch, see, generate, ingest, execute, delegate — each one stating its own limits, so the model never promises a capability it doesn't have. [[pause:900ms]] Watch lines 29 through 55 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [29, 55] },
        { "id": "spec-method", "label": "The Method", "cue": "Search first for anything recent, Python for anything numeric — and no tool theater.", "script": "Search first for anything recent, Python for anything numeric — and no tool theater. [[pause:900ms]] Watch lines 56 through 70 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [56, 70] },
        { "id": "spec-style", "label": "Style", "cue": "“Match the user's register.” And be willing to say “I don't know” before guessing.", "script": "“Match the user's register.” And be willing to say “I don't know” before guessing. [[pause:900ms]] Watch lines 71 through 79 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [71, 79] },
        { "id": "spec-rules", "label": "The rules that fence it in", "cue": "The sandbox can't reach your host — so it never promises to save anything to ~/Downloads. And three failure rules: a dead container runtime, a missing OPENAI_API_KEY, an unconfigured WebSearch — each one relayed, not papered over.", "script": "The sandbox can't reach your host — so it never promises to save anything to ~/Downloads. And three failure rules: a dead container runtime, a missing OPENAI_API_KEY, an unconfigured WebSearch — each one relayed, not papered over. [[pause:900ms]] Watch lines 80 through 102 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [80, 102] },
        { "id": "spec-researcher", "label": "One sub-agent: web-researcher", "cue": "3-5 searches, 2-3 fetches, back with a cited brief under 250 words — in its own context.", "script": "3-5 searches, 2-3 fetches, back with a cited brief under 250 words — in its own context. [[pause:900ms]] Watch lines 103 through 129 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [103, 129] },
        { "id": "spec-tools", "label": "Nine tools", "cue": "Notice what's absent: no read, no write, no edit, no bash on your machine.", "script": "Notice what's absent: no read, no write, no edit, no bash on your machine. [[pause:900ms]] Watch lines 130 through 140 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [130, 140] },
        { "id": "spec-perms-web", "label": "Web, vision, images, ingest", "cue": "All always-on — this is a research-and-chat agent, and none of it touches your filesystem.", "script": "All always-on — this is a research-and-chat agent, and none of it touches your filesystem. [[pause:900ms]] Watch lines 141 through 158 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [141, 158] },
        { "id": "spec-perms-sandbox", "label": "Code execution, always-on", "cue": "Python, JavaScript and Shell need no prompt because the isolation boundary is a container, not a process — network=none, read-only root, pinned image allowlist. Your host filesystem is not reachable from them; the cost is that they need a container runtime up.", "script": "Python, JavaScript and Shell need no prompt because the isolation boundary is a container, not a process — network=none, read-only root, pinned image allowlist. Your host filesystem is not reachable from them; the cost is that they need a container runtime up. [[pause:900ms]] Watch lines 159 through 170 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [159, 170] },
        { "id": "spec-perms-task", "label": "And the sub-agent dispatch grant", "cue": "One more rule, easy to miss: Task. That's the grant the /browse beat spends two groups from now — no Task rule, no sub-agent fan-out.", "script": "One more rule, easy to miss: Task. That's the grant the /browse beat spends two groups from now — no Task rule, no sub-agent fan-out. [[pause:900ms]] Watch lines 171 through 173 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [171, 173] },
        { "id": "spec-compaction", "label": "Compact on a cheap model", "cue": "Older turns get summarized on Haiku, so a long conversation stays cheap.", "script": "Older turns get summarized on Haiku, so a long conversation stays cheap. [[pause:900ms]] Watch lines 174 through 176 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [174, 176] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“lint: clean — no findings.” Parse, the IR passes, and the scope audit — nothing emitted yet.", "script": "“lint: clean — no findings.” Parse, the IR passes, and the scope audit — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-tools", "label": "Audit its own tool grants", "cue": "The keyword ranker wants to hand it read, write, edit and bash. This spec withholds them.", "script": "The keyword ranker wants to hand it read, write, edit and bash. This spec withholds them. [[pause:1.5s]] This is an offline proof and should complete successfully. Keyword-ranks builtins against agent.instructions; the host-side tools it proposes are deliberately not granted.", "action": "command", "command": "crewhaus tools suggest live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "keyword-ranks builtins against agent.instructions; the host-side tools it proposes are deliberately not granted" } },
        { "id": "prove-ir", "label": "Emit the lowered IR", "cue": "The spec is sugar. Lower it to JSON — too long for a terminal, so send it to a file.", "script": "The spec is sugar. Lower it to JSON — too long for a terminal, so send it to a file. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. Writes the lowered IR (~96 lines) instead of paging it; the inlined instructions string is the long part.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir > live/ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "writes the lowered IR (~96 lines) instead of paging it; the inlined instructions string is the long part", "produces": ["live/ir.json"] } },
        { "id": "prove-open-ir", "label": "Open the IR", "cue": "Nine tools, ten permission rules, one sub-agent, the compaction model — what the compiler actually sees, and nothing invented.", "script": "Nine tools, ten permission rules, one sub-agent, the compaction model — what the compiler actually sees, and nothing invented. [[pause:1.2s]] The editor opens live slash ir.json now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/ir.json" },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "--check asserts the shape, installs the deps, and boots it: GREEN with no key.", "script": "--check asserts the shape, installs the deps, and boots it: GREEN with no key. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok; install ok; boot gated at the credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok; install ok; boot gated at the credentials gate", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "Nine tools, one sub-agent, every permission rule — one TypeScript program you own.", "script": "Nine tools, one sub-agent, every permission rule — one TypeScript program you own. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-repl", "label": "Launch the chat REPL", "cue": "[needs-key] crewhaus run compiles in memory and drops into a streaming chat loop.", "script": "Crewhaus run compiles in memory and drops into a streaming chat loop. [[pause:1.8s]] This is a live, credentialed step and is intentionally excluded from offline replay. Interactive REPL; needs Anthropic credentials. The boot banner is five lines before “agent ready”: continuity on · 4 skills · 12 slash commands (the starter's six plus continuity's six) · 1 sub-agent · “[sandbox] assuming docker”.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "interactive REPL; needs Anthropic credentials. The boot banner is five lines before “agent ready”: continuity on · 4 skills · 12 slash commands (the starter's six plus continuity's six) · 1 sub-agent · “[sandbox] assuming docker”" } },
        { "id": "run-chat", "label": "Just talk to it", "cue": "[needs-key] Plain chat — no tools fire. The model just answers.", "script": "Plain chat — no tools fire. The model just answers. [[pause:1.2s]] The driver sends the next user turn into the active session: “what's the capital of Burkina Faso?” This keeps the proof inside the current conversation, so any continuity in the answer is visible.", "action": "input", "text": "what's the capital of Burkina Faso?", "verify": { "mode": "needs-key" } },
        { "id": "run-math", "label": "Make it do arithmetic", "cue": "[needs-key] It reaches for Python on its own — /code is optional. Watching it decide is the demo.", "script": "It reaches for Python on its own — /code is optional. Watching it decide is the demo. [[pause:1.2s]] The driver sends the next user turn into the active session: “what's 17! / 12!? show your work”. The Python tool shells out to `docker run python:3.13-slim`.", "action": "input", "text": "what's 17! / 12!? show your work", "verify": { "mode": "needs-key", "note": "the Python tool shells out to `docker run python:3.13-slim` — needs the backend the setup-sandbox beat probed, and the image already pulled, or the first call blocks on a multi-hundred-MB pull mid-take" } },
        { "id": "run-summarize", "label": "Summarize a URL", "cue": "[needs-key] Fetches the page and returns a three-sentence summary.", "script": "Fetches the page and returns a three-sentence summary. [[pause:1.2s]] The driver sends the next user turn into the active session: “/summarize https://en.wikipedia.org/wiki/Burkina_Faso”. This keeps the proof inside the current conversation, so any continuity in the answer is visible.", "action": "input", "text": "/summarize https://en.wikipedia.org/wiki/Burkina_Faso", "verify": { "mode": "needs-key" } },
        { "id": "run-browse", "label": "Fan out the researcher", "cue": "[needs-key] web-researcher returns a TL;DR, five cited facts, open questions, and sources.", "script": "Web-researcher returns a TL;DR, five cited facts, open questions, and sources. [[pause:1.2s]] The driver sends the next user turn into the active session: “/browse latest news on the Anthropic Claude 4.7 release”. WebSearch needs CREWHAUS_SEARCH_PROVIDER + CREWHAUS_SEARCH_API_KEY as well as the model key.", "action": "input", "text": "/browse latest news on the Anthropic Claude 4.7 release", "verify": { "mode": "needs-key", "note": "WebSearch needs CREWHAUS_SEARCH_PROVIDER + CREWHAUS_SEARCH_API_KEY as well as the model key" } },
        { "id": "run-imagine", "label": "Generate an image", "cue": "[needs-key] DALL-E when OPENAI_API_KEY is set; otherwise the mock placeholder, surfaced verbatim — because Setup exported the fallback.", "script": "DALL-E when OPENAI_API_KEY is set; otherwise the mock placeholder, surfaced verbatim — because Setup exported the fallback. [[pause:1.2s]] The driver sends the next user turn into the active session: “/imagine a lobster wearing a top hat, oil-painting style”. ImageGenerate resolves openai when OPENAI_API_KEY is set.", "action": "input", "text": "/imagine a lobster wearing a top hat, oil-painting style", "verify": { "mode": "needs-key", "note": "ImageGenerate resolves openai when OPENAI_API_KEY is set. The placeholder is NOT a built-in default: it comes from the setup-image-provider beat's CREWHAUS_IMAGE_PROVIDER=mock — without that export the tool throws “OPENAI_API_KEY is not set — required for provider=openai”" } },
        { "id": "run-exit", "label": "Exit the REPL", "cue": "[needs-key] “exit” returns to the shell.", "script": "“exit” returns to the shell. [[pause:1.2s]] The driver sends exit to close the active REPL cleanly. The terminal returns to a normal shell while the generated artifacts and session history remain available.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · the same agent, somewhere else",
      "beats": [
        { "id": "extra-skill", "label": "A skill the model loads itself", "cue": "Markdown with triggers — it self-loads when a question smells like research. No recompile.", "script": "Markdown with triggers — it self-loads when a question smells like research. No recompile. [[pause:1.2s]] The editor opens .crewhaus slash skills slash research topic slash SKILL.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": ".crewhaus/skills/research-topic/SKILL.md" },
        { "id": "extra-command", "label": "A slash command is a Markdown file", "cue": "Six ship in .crewhaus/commands/ — /imagine is just a prompt the model follows literally.", "script": "Six ship in .crewhaus/commands/ — /imagine is just a prompt the model follows literally. [[pause:1.2s]] The editor opens .crewhaus slash commands slash imagine.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": ".crewhaus/commands/imagine.md" },
        { "id": "extra-plugin", "label": "Export it as a Claude Code plugin", "cue": "The same spec, emitted as a plugin dir — four files: the manifest, a README, the researcher as a Claude Code sub-agent, and the persona itself as a skill. The authored skills and the six slash commands stay behind: this projects the spec, not the .crewhaus/ tree.", "script": "The same spec, emitted as a plugin dir — four files: the manifest, a README, the researcher as a Claude Code sub-agent, and the persona itself as a skill. The authored skills and the six slash commands stay behind: this projects the spec, not the .crewhaus/ tree. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. “exported hello-prochat (cli) … 4 files, smoke check ✓” — .claude-plugin/plugin.json, README.md, agents/web-researcher.md, skills/hello-prochat/SKILL.md (that skill is agent.instructions, not the authored research-topic one).", "action": "command", "command": "crewhaus export claude-plugin live/crewhaus.yaml --out live/plugin", "verify": { "mode": "offline", "expectedExit": 0, "note": "“exported hello-prochat (cli) … 4 files, smoke check ✓” — .claude-plugin/plugin.json, README.md, agents/web-researcher.md, skills/hello-prochat/SKILL.md (that skill is agent.instructions, not the authored research-topic one)", "produces": ["live/plugin/.claude-plugin/plugin.json", "live/plugin/agents/web-researcher.md"] } },
        { "id": "extra-mcp", "label": "Or serve it as an MCP tool", "cue": "[needs-key] serve --mcp projects the whole agent as one MCP tool — `chat` — that your IDE can call. It prints two lines and then blocks: the terminal belongs to the server until you Ctrl-C it.", "script": "Serve --mcp projects the whole agent as one MCP tool — `chat` — that your IDE can call. It prints two lines and then blocks: the terminal belongs to the server until you Ctrl-C it. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Long-running stdio MCP server: prints the continuity line + “[serve] MCP stdio server for \"hello-prochat\" ready (tools: chat)”, then blocks until SIGINT (“[serve] stopped.”).", "action": "command", "command": "crewhaus serve --mcp live/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "long-running stdio MCP server: prints the continuity line + “[serve] MCP stdio server for \"hello-prochat\" ready (tools: chat)”, then blocks until SIGINT (“[serve] stopped.”). Every tool call runs the agent, so it needs provider credentials — and the drive closes on the editor beat below, not on this terminal" } },
        { "id": "extra-open-plugin", "label": "Close on the exported researcher", "cue": "Your sub-agent, now a Claude Code sub-agent. One spec, more than one runtime.", "script": "Your sub-agent, now a Claude Code sub-agent. One spec, more than one runtime. [[pause:1.2s]] The editor opens live slash plugin slash agents slash web researcher.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/plugin/agents/web-researcher.md" }
      ]
    }
  ]
}
