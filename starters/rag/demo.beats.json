{
  "title": "hello-rag — grounded answers over an indexed corpus (target: pipeline)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-readme", "label": "Open the starter README", "cue": "“An agent grounded in a vector store via the Retrieve tool.”", "script": "An agent grounded in a vector store via the Retrieve tool. [[pause:1.2s]] The editor opens README.md now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "README.md" },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed crewhaus.yaml stays untouched.", "script": "Live/ is scratch — the committed crewhaus.yaml stays untouched. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the run beats.", "script": "Sources demos/.env for the run beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The spec — retrieval is configuration",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“target: pipeline” — the component-DAG runtime, the RAG shape.", "script": "“target: pipeline” — the component-DAG runtime, the RAG shape. [[pause:900ms]] Watch the driver type lines 1 through 2 of crewhaus.yaml, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [1, 2], "replace": true },
        { "id": "spec-agent", "label": "Add the agent block", "cue": "Retrieve first, answer in 2–3 sentences, cite by [N] — and refuse what the corpus misses.", "script": "Retrieve first, answer in 2–3 sentences, cite by [N] — and refuse what the corpus misses. [[pause:900ms]] Watch lines 3 through 11 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [3, 11] },
        { "id": "spec-retrieve", "label": "Wire the retriever", "cue": "A deterministic embedder into an in-memory store — and top-2, deliberately well under the seven chunks this corpus indexes to. k near the store size isn't retrieval, it's “send everything”.", "script": "A deterministic embedder into an in-memory store — and top-2, deliberately well under the seven chunks this corpus indexes to. k near the store size isn't retrieval, it's “send everything”. [[pause:900ms]] Watch lines 12 through 17 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [12, 17] },
        { "id": "spec-permissions", "label": "Pre-allow Retrieve", "cue": "Grounding shouldn't stop to ask permission — one alwaysAllow rule.", "script": "Grounding shouldn't stop to ask permission — one alwaysAllow rule. [[pause:900ms]] Watch lines 18 through 22 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [18, 22] },
        { "id": "spec-indexing", "label": "Configure the chunker", "cue": "Fixed 400-character chunks, zero overlap. The chunker is spec, not code.", "script": "Fixed 400-character chunks, zero overlap. The chunker is spec, not code. [[pause:900ms]] Watch lines 23 through 27 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [23, 27] },
        { "id": "spec-corpus-shapes", "label": "Add the first document", "cue": "The corpus is inline: document one is the roster the demo asks about — all fourteen target shapes.", "script": "The corpus is inline: document one is the roster the demo asks about — all fourteen target shapes. [[pause:900ms]] Watch lines 28 through 35 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [28, 35] },
        { "id": "spec-corpus-run", "label": "Add the run-vs-compile document", "cue": "Document two: which shapes `crewhaus run` accepts, and what every other shape does instead.", "script": "Document two: which shapes `crewhaus run` accepts, and what every other shape does instead. [[pause:900ms]] Watch lines 36 through 43 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [36, 43] },
        { "id": "spec-corpus-knobs", "label": "Add the retrieval-knobs document", "cue": "Document three is the retrieval knobs themselves — 513 characters, so the 400-char window cuts it in two. That's the one place chunking is visible.", "script": "Document three is the retrieval knobs themselves — 513 characters, so the 400-char window cuts it in two. That's the one place chunking is visible. [[pause:900ms]] Watch lines 44 through 54 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [44, 54] },
        { "id": "spec-corpus-safety", "label": "Add the section-18 document", "cue": "Document four: the production safety floor — sandbox, code execution, injection detector.", "script": "Document four: the production safety floor — sandbox, code execution, injection detector. [[pause:900ms]] Watch lines 55 through 62 of crewhaus.yaml become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [55, 62] },
        { "id": "spec-corpus-sections", "label": "Add the last two documents", "cue": "Sections 19 and 20 — the graph and managed shapes. Six documents, no ingestion step.", "script": "Sections 19 and 20 — the graph and managed shapes. Six documents, no ingestion step. [[pause:900ms]] Watch lines 63 through 75 of crewhaus.yaml become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "crewhaus.yaml", "sourceLines": [63, 75] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Lower the spec to IR", "cue": "Park the IR in scratch — it's longer than a terminal screen, and it reads better in the editor.", "script": "Park the IR in scratch — it's longer than a terminal screen, and it reads better in the editor. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. IR for target pipeline, redirected to scratch (55 lines) — the next beat opens it.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir > live/ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "IR for target pipeline, redirected to scratch (55 lines) — the next beat opens it", "produces": ["live/ir.json"] } },
        { "id": "prove-open-ir", "label": "Read the IR", "cue": "indexing.documents — all six of them, inline. The compiler sees documents, not a directory.", "script": "Indexing.documents — all six of them, inline. The compiler sees documents, not a directory. [[pause:1.2s]] The editor opens live slash ir.json now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "live/ir.json" },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN assertion ok (4 anchors); install ok; boot gated at the credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated at the credentials gate. On CLI 0.4.0 --check is also the only thing that writes the bundle's pinned package.json + node_modules; measured: a plain `-o dist` emits agent.ts and README.md only, and `bun --no-install dist/agent.ts` then fails with `Cannot find module '@crewhaus/runtime-core'`.", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "The indexing DAG is right there: chunk → embed → store, wired at boot.", "script": "The indexing DAG is right there: chunk → embed → store, wired at boot. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-refuse", "label": "Try crewhaus run", "cue": "“run supports target cli or browser” — pipeline is compile-only, so the bundle is the runtime. That's why the README's Run-it block compiles first.", "script": "“run supports target cli or browser” — pipeline is compile-only, so the bundle is the runtime. That's why the README's Run-it block compiles first. [[pause:1.5s]] The refusal is the proof in this beat, so the verifier expects exit code 1. Deliberate rejection, exit 1: `crewhaus run` supports cli|browser; other shapes are compile-only. The corpus's own run-vs-compile document says the same thing, so the next beats can ask the agent about it.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection, exit 1: `crewhaus run` supports cli|browser; other shapes are compile-only. The corpus's own run-vs-compile document says the same thing, so the next beats can ask the agent about it." } },
        { "id": "run-bundle", "label": "Boot the bundle", "cue": "[needs-key] “indexed 7 chunks” scrolls past first — indexing runs before the chat loop, in-process.", "script": "“indexed 7 chunks” scrolls past first — indexing runs before the chat loop, in-process. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Indexes the corpus offline (prints `[pipeline] indexed 7 chunks`), then needs Anthropic credentials for the loop.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "indexes the corpus offline (prints `[pipeline] indexed 7 chunks`), then needs Anthropic credentials for the loop" } },
        { "id": "run-ask", "label": "Ask what it was indexed on", "cue": "[needs-key] Retrieve fires first, and the answer is the corpus read back — fourteen shapes, cited [1]. Grounding means the document is the source of truth, not the model.", "script": "Retrieve fires first, and the answer is the corpus read back — fourteen shapes, cited [1]. Grounding means the document is the source of truth, not the model. [[pause:1.2s]] The driver sends the next user turn into the active session: “Which target harness shapes does CrewHaus Factory support?” Measured on CLI 0.4.0 with a live key: the model retrieves the target-shapes chunk (rank 1 of 7) and lists all fourteen shapes with a [1] citation.", "action": "input", "text": "Which target harness shapes does CrewHaus Factory support?", "verify": { "mode": "needs-key", "note": "measured on CLI 0.4.0 with a live key: the model retrieves the target-shapes chunk (rank 1 of 7) and lists all fourteen shapes with a [1] citation" } },
        { "id": "run-ask-narrow", "label": "Ask a narrower question", "cue": "[needs-key] Different question, different chunks: top-2 of 7 lands on section-19, and the citation names where the sentence came from.", "script": "Different question, different chunks: top-2 of 7 lands on section-19, and the citation names where the sentence came from. [[pause:1.2s]] The driver sends the next user turn into the active session: “What does the graph target use for checkpoints?” Measured: this question ranks section-19 first and target-shapes second, where the headline question ranks target-shapes first.", "action": "input", "text": "What does the graph target use for checkpoints?", "verify": { "mode": "needs-key", "note": "measured: this question ranks section-19 first and target-shapes second, where the headline question ranks target-shapes first — the two beats retrieve different sets, which is the point of defaultK: 2 over a 7-chunk store" } }
      ]
    },
    {
      "title": "One more thing · the corpus is the boundary",
      "beats": [
        { "id": "extra-out-of-corpus", "label": "Ask it something outside the corpus", "cue": "[needs-key] “I can only answer questions about the indexed docs” — retrieval has no relevance floor, so the refusal is carried by the instructions block, not by the score.", "script": "“I can only answer questions about the indexed docs” — retrieval has no relevance floor, so the refusal is carried by the instructions block, not by the score. [[pause:1.2s]] The driver sends the next user turn into the active session: “What's the weather in Tokyo right now?” Measured: the Retrieve tool always returns k hits regardless of distance, so the refusal is the agent.instructions clause doing the work.", "action": "input", "text": "What's the weather in Tokyo right now?", "verify": { "mode": "needs-key", "note": "measured: the Retrieve tool always returns k hits regardless of distance, so the refusal is the agent.instructions clause doing the work" } },
        { "id": "extra-exit", "label": "Exit", "cue": "[needs-key] Back to the shell.", "script": "The grounded-answering session is complete, so we close the pipeline REPL cleanly. [[pause:1.2s]] The driver sends exit to close the active REPL cleanly. The terminal returns to a normal shell while the generated artifacts and session history remain available.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    }
  ]
}
