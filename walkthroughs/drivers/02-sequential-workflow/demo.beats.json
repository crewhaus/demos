{
  "title": "Recipe 02 — Sequential Workflow (a deterministic pipeline, step by step)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“Recipe 02 is the shape you reach for when you want predictability over agentic flexibility — extract, transform, format.”", "script": "Recipe 02 is the shape you reach for when you want predictability over agentic flexibility — extract, transform, format. [[pause:1.2s]] The editor opens 02 sequential workflow.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "../../02-sequential-workflow.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed recipe is never touched by a take.", "script": "Live/ is scratch — the committed recipe is never touched by a take. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the boot beats.", "script": "Sources demos/.env for the boot beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The smallest spec — two steps, in order",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“model: is top-level, not under an agent: block. A workflow has no single agent — it has a sequence of steps.”", "script": "Model: is top-level, not under an agent: block. A workflow has no single agent — it has a sequence of steps. [[pause:900ms]] Watch the driver type lines 76 through 78 of 02 sequential workflow.md, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [76, 78], "replace": true },
        { "id": "spec-permissions", "label": "Allow Bash explicitly", "cue": "“Steps run single-turn — there is no interactive surface to prompt on, so anything that would ask becomes deny.” Without these three lines, step 1 never runs ls -la.", "script": "“Steps run single-turn — there is no interactive surface to prompt on, so anything that would ask becomes deny.” Without these three lines, step 1 never runs ls -la. [[pause:900ms]] Watch lines 79 through 81 of 02 sequential workflow.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [79, 81] },
        { "id": "spec-step-1", "label": "Step 1 · list-files", "cue": "“Run ls -la, report in 1–3 short sentences, do not editorialize beyond what the listing shows.” Tools are granted per step: this one gets bash.", "script": "“Run ls -la, report in 1–3 short sentences, do not editorialize beyond what the listing shows.” Tools are granted per step: this one gets bash. [[pause:900ms]] Watch lines 82 through 89 of 02 sequential workflow.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [82, 89] },
        { "id": "spec-step-2", "label": "Step 2 · summarize", "cue": "“You will receive the previous step's directory listing as context.” That threading is the whole shape — and step 2 calls no tools.", "script": "“You will receive the previous step's directory listing as context.” That threading is the whole shape — and step 2 calls no tools. [[pause:900ms]] Watch lines 90 through 94 of 02 sequential workflow.md become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [90, 94] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“Two steps, each with its own instructions and tool grant. That JSON is the whole program.”", "script": "Two steps, each with its own instructions and tool grant. That JSON is the whole program. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target workflow (2 steps).", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target workflow (2 steps)" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (3 anchors); install ok; boot gated at the provider credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (3 anchors); install ok; boot gated at the provider credentials gate", "produces": ["live/dist/agent.ts", "live/dist/package.json"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted pipeline", "cue": "“Read how step 1's terminal text is seeded into step 2 — the recipe's ‘threading forward' is a few lines of generated code, not a framework.”", "script": "Read how step 1's terminal text is seeded into step 2 — the recipe's ‘threading forward’ is a few lines of generated code, not a framework. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" },
        { "id": "prove-worker", "label": "Try to ship it to the edge", "cue": "“Refused: step 1 runs a host shell, and a Cloudflare Worker has no shell.” The compiler won't quietly drop the tool to make it fit.", "script": "“Refused: step 1 runs a host shell, and a Cloudflare Worker has no shell.” The compiler won't quietly drop the tool to make it fit. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection, exit 1 — `cf-worker target cannot run 1 host tool(s): tool \"bash\" runs a host shell process`.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-as cf-worker -o live/worker", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection, exit 1 — `cf-worker target cannot run 1 host tool(s): tool \"bash\" runs a host shell process`" } }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL — a workflow isn't a chat.” Worth saying out loud: the recipe's standalone block reaches for `crewhaus run`, and on 0.4.2 that exits 1 and names the two shapes it does take. Boot the bundle instead.", "script": "“run is the cli/browser REPL — a workflow isn't a chat.” Worth saying out loud: the recipe's standalone block reaches for `crewhaus run`, and on 0.4.2 that exits 1 and names the two shapes it does take. Boot the bundle instead. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection, exit 1 `crewhaus run supports target: cli or browser (got \"workflow\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection, exit 1 — `crewhaus run supports target: cli or browser (got \"workflow\")`. Recipe 02's standalone Run block still prints `bunx crewhaus run crewhaus.yaml`; the repo-root path it offers next (`bun run run starters/workflow`) boots the compiled bundle, which is what this driver does" } },
        { "id": "run-bundle", "label": "Boot the compiled pipeline", "cue": "[needs-key] “Step 1 looks around, step 2 summarizes what it found, and the process exits. Two assistant messages, no user input in between.”", "script": "Step 1 looks around, step 2 summarizes what it found, and the process exits. Two assistant messages, no user input in between. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Runs both steps against Anthropic; deps were installed by the --check beat.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "runs both steps against Anthropic; deps were installed by the --check beat" } },
        { "id": "run-jsonl", "label": "Read each step's terminal text back", "cue": "[needs-key] “Every step contributes one assistant_message — this projects the run to just the text each step ended on.”", "script": "Every step contributes one assistant_message — this projects the run to just the text each step ended on. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. The recipe's own JSONL command against the newest session; the session file only exists after the keyed run above.", "action": "command", "command": "jq -r 'select(.kind == \"assistant_message\") | .payload.content[-1].text' \"$(ls -t .crewhaus/sessions/sess_*.jsonl | head -1)\"", "verify": { "mode": "needs-key", "note": "the recipe's own JSONL command against the newest session; the session file only exists after the keyed run above" } }
      ]
    },
    {
      "title": "One more thing · Step-level overrides — the cost lever",
      "beats": [
        { "id": "steps-head", "label": "A second workflow, same shape", "cue": "“extract-then-write — same two-step spine, but each step picks its own model.”", "script": "Extract-then-write — same two-step spine, but each step picks its own model. [[pause:900ms]] Watch the driver type lines 162 through 165 of 02 sequential workflow.md, rebuilding live slash steps.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [162, 165], "target": "live/steps.yaml", "replace": true },
        { "id": "steps-body", "label": "Haiku extracts, the workflow model writes", "cue": "“Extraction is structural — haiku is plenty. Synthesis inherits the workflow's sonnet.” That swap is the single biggest workflow cost lever.", "script": "“Extraction is structural — haiku is plenty. Synthesis inherits the workflow's sonnet.” That swap is the single biggest workflow cost lever. [[pause:900ms]] Watch lines 166 through 178 of 02 sequential workflow.md become the next block of live slash steps.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "../../02-sequential-workflow.md", "sourceLines": [166, 178], "target": "live/steps.yaml" },
        { "id": "steps-lint", "label": "Lint the override spec", "cue": "“Two models in one spec, still clean — and no permissions block, because read and grep never ask.”", "script": "Two models in one spec, still clean — and no permissions block, because read and grep never ask. [[pause:1.5s]] This is an offline proof and should complete successfully. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/steps.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "steps-check", "label": "Check the override bundle", "cue": "“Same money beat on the second spec: GREEN with no key.”", "script": "Same money beat on the second spec: GREEN with no key. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. GREEN — assertion ok (3 anchors); install ok; boot gated at the provider credentials gate.", "action": "command", "command": "crewhaus compile live/steps.yaml -o live/dist-steps --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (3 anchors); install ok; boot gated at the provider credentials gate", "produces": ["live/dist-steps/agent.ts"] } },
        { "id": "steps-grep", "label": "Find both models in the emitted code", "cue": "“Per-step model routing isn't runtime magic — it's two literals in the bundle you own.”", "script": "Per-step model routing isn't runtime magic — it's two literals in the bundle you own. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Step `extract` carries claude-haiku-4-5-20251001, step `prioritize` inherits claude-sonnet-5 — both emitted as plain model fields.", "action": "command", "command": "grep -nE 'claude-haiku-4-5-20251001|claude-sonnet-5' live/dist-steps/agent.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "step `extract` carries claude-haiku-4-5-20251001, step `prioritize` inherits claude-sonnet-5 — both emitted as plain model fields" } }
      ]
    }
  ]
}
