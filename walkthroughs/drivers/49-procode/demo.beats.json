{
  "title": "Recipe 49 — Pro-grade Coder (à la Claude Code), as a spec you own",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“A tier-1 coding agent — sub-agents, permission tiers, compaction — and none of it is hidden in a runtime.”", "script": "We’re opening the full walkthrough before changing anything, keeping the explanation and the implementation side by side. [[pause:1s]] A tier-1 coding agent — sub-agents, permission tiers, compaction — and none of it is hidden in a runtime.", "action": "open", "file": "../../49-procode.md", "preview": true },
        { "id": "setup-reset", "label": "Blank the scratch file", "cue": "live/ is scratch — the committed showcase is never touched by a take.", "script": "We’re preparing a clean scratch state for “Blank the scratch file,” so this section starts from a repeatable baseline. [[pause:800ms]] live/ is scratch — the committed showcase is never touched by a take. The committed starter and walkthrough stay unchanged, which makes another take reproducible.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — Step 1 says run it before you read it.", "script": "We’re loading the demo environment into this terminal session; credentials remain in the shell and are never written into the spec. [[pause:1.2s]] Sources demos/.env — Step 1 says run it before you read it. Later credentialed beats reuse this exact shell environment without persisting the secret in the manifest.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · Step 1 — run it first, read it after",
      "beats": [
        { "id": "spec-open", "label": "Open the committed spec", "cue": "“Nearly six hundred lines. You don't type this on camera — you read it, and the recipe walks you through it section by section.”", "script": "The editor is opening the scratch CrewHaus spec for “Open the committed spec,” so we can inspect the concrete artifact on screen. [[pause:1s]] Nearly six hundred lines. You don't type this on camera — you read it, and the recipe walks you through it section by section.", "action": "open", "file": "../../../starters/showcases/procode/crewhaus.yaml" },
        { "id": "spec-lint", "label": "Lint it in place", "cue": "“Clean, at that size, with sub-agents and permission tiers. That's what a schema buys you.”", "script": "The terminal is running “Lint it in place” now; its output and exit status are the evidence for this beat. [[pause:1s]] Clean, at that size, with sub-agents and permission tiers. That's what a schema buys you.", "action": "command", "command": "crewhaus lint ../../../starters/showcases/procode/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "spec-check", "label": "Compile it and check it", "cue": "“--check: GREEN, gated at the model credential. Six hundred lines of spec, one emitted program.”", "script": "The terminal is running “Compile it and check it” now; its output and exit status are the evidence for this beat. [[pause:1s]] --check: GREEN, gated at the model credential. Six hundred lines of spec, one emitted program.", "action": "command", "command": "crewhaus compile ../../../starters/showcases/procode/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok; install ok; boot gated at the provider credentials gate", "produces": ["live/dist/agent.ts"] } }
      ]
    },
    {
      "title": "Beat 2 · Steps 2–3 — the sections that make it tier-1",
      "beats": [
        {"id": "sec-subagents", "label": "Type the sub-agents block", "cue": "“Step 2: each sub-agent gets its own tool catalog, filtered from the parent's. Delegation with a smaller blast radius.”", "script": "The driver is copying the exact source-backed lines for “Type the sub-agents block” into the scratch artifact now. [[pause:1.2s]] Step 2: each sub-agent gets its own tool catalog, filtered from the parent's. Delegation with a smaller blast radius.", "action": "type", "source": "../../49-procode.md", "sourceLines": [103, 127], "target": "live/sub-agents.yaml", "replace": true},
        { "id": "sec-open-subagents", "label": "Read it", "cue": "“Note the tools list — the Task tool has to be in `tools`, not just permissions, or the child catalog comes back empty.”", "script": "The editor is opening the sub agents configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] Note the tools list — the Task tool has to be in `tools`, not just permissions, or the child catalog comes back empty.", "action": "open", "file": "live/sub-agents.yaml" },
        {"id": "sec-permissions", "label": "Type the permission tiers", "cue": "“Step 3: allow, ask, deny. Deny always wins, and an allow rule can never widen a deny.”", "script": "The driver is copying the exact source-backed lines for “Type the permission tiers” into the scratch artifact now. [[pause:1.2s]] Step 3: allow, ask, deny. Deny always wins, and an allow rule can never widen a deny.", "action": "type", "source": "../../49-procode.md", "sourceLines": [145, 168], "target": "live/permissions.yaml", "replace": true},
        { "id": "sec-open-permissions", "label": "Read it", "cue": "“This is the tier the security fabric recipe points back at as the runnable example.”", "script": "The editor is opening the permissions configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] This is the tier the security fabric recipe points back at as the runnable example.", "action": "open", "file": "live/permissions.yaml" },
        {"id": "sec-compaction", "label": "Type the compaction block", "cue": "“Step 5: compaction is not optional at this size — a long coding session will blow the window without it.”", "script": "The driver is copying the exact source-backed lines for “Type the compaction block” into the scratch artifact now. [[pause:1.2s]] Step 5: compaction is not optional at this size — a long coding session will blow the window without it.", "action": "type", "source": "../../49-procode.md", "sourceLines": [202, 203], "target": "live/compaction.yaml", "replace": true},
        { "id": "sec-grep", "label": "Find them in the emitted bundle", "cue": "“Every one of those sections turned into ordinary code you can read.”", "script": "The terminal is running “Find them in the emitted bundle” now; its output and exit status are the evidence for this beat. [[pause:1s]] Every one of those sections turned into ordinary code you can read. Expected terminal result: the three sections, as generated code.", "action": "command", "command": "grep -inE 'subAgent|permission|compact' live/dist/agent.ts > live/wired.txt", "verify": { "mode": "offline", "expectedExit": 0, "note": "the three sections, as generated code", "produces": ["live/wired.txt"] } },
        { "id": "sec-open-wired", "label": "Read what got wired", "cue": "“No framework indirection between the YAML and the behaviour.”", "script": "The editor is opening the wired artifact for “Read what got wired,” so we can inspect the concrete artifact on screen. [[pause:1s]] No framework indirection between the YAML and the behaviour. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "live/wired.txt" }
      ]
    },
    {
      "title": "Beat 3 · Step 1, for real",
      "beats": [
        { "id": "run-repl", "label": "Launch it", "cue": "[needs-key] “Point it at this repo and ask the recipe's own opening question.”", "script": "The terminal is starting the credentialed step “Launch it” now; it may make a real provider or service call. [[pause:1.2s]] Point it at this repo and ask the recipe's own opening question. Operational note: interactive REPL; needs a live model credential.", "action": "command", "command": "crewhaus run ../../../starters/showcases/procode/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "interactive REPL; needs a live model credential" } },
        { "id": "run-explore", "label": "Ask it to explore", "cue": "[needs-key] “Watch it delegate the wide reads to a sub-agent instead of filling its own context.”", "script": "We’re sending the next prompt for “Ask it to explore” to the process already running in the demo terminal. [[pause:1.2s]] Watch it delegate the wide reads to a sub-agent instead of filling its own context.", "action": "input", "text": "explore this repo and tell me what it does in three sentences", "verify": { "mode": "needs-key" } },
        { "id": "run-exit", "label": "Exit", "cue": "[needs-key] “exit” returns to the shell.", "script": "We’re sending exit to the active REPL, closing the interactive process without discarding the terminal transcript. [[pause:1.2s]] “exit” returns to the shell. The completed interaction stays visible in the terminal for review.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · Step 6 — swap the model",
      "beats": [
        { "id": "swap-open", "label": "Read the provider table", "cue": "“One line changes the provider. The rest of the six hundred is provider-agnostic.”", "script": "The editor is opening the walkthrough for “Read the provider table,” so we can inspect the concrete artifact on screen. [[pause:1s]] One line changes the provider. The rest of the six hundred is provider-agnostic.", "action": "open", "file": "../../49-procode.md", "preview": true },
        { "id": "swap-grep", "label": "Find the fallback chain", "cue": "“model_fallbacks is the long-unattended-run insurance: if the primary breaker opens, turns retry down the chain instead of dying mid-goal.”", "script": "The terminal is running “Find the fallback chain” now; its output and exit status are the evidence for this beat. [[pause:1s]] model_fallbacks is the long-unattended-run insurance: if the primary breaker opens, turns retry down the chain instead of dying mid-goal.", "action": "command", "command": "grep -n -A 2 'model_fallbacks' ../../../starters/showcases/procode/crewhaus.yaml > live/fallbacks.txt", "verify": { "mode": "offline", "expectedExit": 0, "note": "the committed failover chain", "produces": ["live/fallbacks.txt"] } },
        { "id": "swap-read", "label": "Read it", "cue": "“Primary and fallback, both current-generation. That's the whole failover story.”", "script": "The editor is opening the fallbacks artifact for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] Primary and fallback, both current-generation. That's the whole failover story.", "action": "open", "file": "live/fallbacks.txt" }
      ]
    }
  ]
}
