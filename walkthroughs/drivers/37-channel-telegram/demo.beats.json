{
  "title": "Recipe 37 — Channel: Telegram (secret-token auth, topic keying, dedup by update_id)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“Recipe 03 taught the channel shape. Recipe 37 is only the Telegram deltas — how it authenticates, how it keys a conversation, how it dedups.”", "script": "We’re opening the full walkthrough before changing anything, keeping the explanation and the implementation side by side. [[pause:1s]] Recipe 03 taught the channel shape. Recipe 37 is only the Telegram deltas — how it authenticates, how it keys a conversation, how it dedups.", "action": "open", "file": "../../37-channel-telegram.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed recipe is never touched by a take.", "script": "We’re preparing a clean scratch state for “Start from a blank spec,” so this section starts from a repeatable baseline. [[pause:800ms]] live/ is scratch — the committed recipe is never touched by a take. The committed starter and walkthrough stay unchanged, which makes another take reproducible.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the tokens", "cue": "[needs-key] Sources demos/.env — Step 1's bot token and secret token, plus the API key for the boot beat.", "script": "We’re loading the demo environment into this terminal session; credentials remain in the shell and are never written into the spec. [[pause:1.2s]] Sources demos/.env — Step 1's bot token and secret token, plus the API key for the boot beat.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env (TELEGRAM_BOT_TOKEN, TELEGRAM_SECRET_TOKEN, ANTHROPIC_AUTH_TOKEN)" } }
      ]
    },
    {
      "title": "Beat 1 · Step 2 — the spec",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“target: channel. Not a REPL — a daemon that waits for other people to talk to it.”", "script": "The driver is copying the exact source-backed lines for “Name it, pick the shape” into the scratch artifact now. [[pause:1.2s]] target: channel. Not a REPL — a daemon that waits for other people to talk to it.", "action": "type", "source": "../../37-channel-telegram.md", "sourceLines": [53, 54], "replace": true },
        { "id": "spec-agent", "label": "Add the agent", "cue": "“Reply concisely, one to two sentences” — the same agent block every other target uses, plus one tool.", "script": "The driver is copying the exact source-backed lines for “Add the agent” into the scratch artifact now. [[pause:1.2s]] “Reply concisely, one to two sentences” — the same agent block every other target uses, plus one tool.", "action": "type", "source": "../../37-channel-telegram.md", "sourceLines": [55, 62] },
        { "id": "spec-channel", "label": "Wire Telegram", "cue": "“The two fields specific to Telegram: botToken to call sendMessage, secretToken to authenticate what comes back.”", "script": "The driver is copying the exact source-backed lines for “Wire Telegram” into the scratch artifact now. [[pause:1.2s]] The two fields specific to Telegram: botToken to call sendMessage, secretToken to authenticate what comes back.", "action": "type", "source": "../../37-channel-telegram.md", "sourceLines": [63, 66] },
        { "id": "spec-routing", "label": "Decide what a conversation is", "cue": "“sessionKey: thread — and in a supergroup forum that means one session per topic, not one per group.”", "script": "The driver is copying the exact source-backed lines for “Decide what a conversation is” into the scratch artifact now. [[pause:1.2s]] sessionKey: thread — and in a supergroup forum that means one session per topic, not one per group.", "action": "type", "source": "../../37-channel-telegram.md", "sourceLines": [67, 68] },
        { "id": "spec-permissions", "label": "Scope it before it goes unattended", "cue": "An unattended daemon can't stop to ask you, so what it may do is declared up front.", "script": "The driver is copying the exact source-backed lines for “Scope it before it goes unattended” into the scratch artifact now. [[pause:1.2s]] An unattended daemon can't stop to ask you, so what it may do is declared up front.", "action": "type", "source": "../../37-channel-telegram.md", "sourceLines": [69, 72] }
      ]
    },
    {
      "title": "Beat 2 · Step 4 — compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "The terminal is running “Lint the spec” now; its output and exit status are the evidence for this beat. [[pause:1s]] Parse plus the IR passes the compile path skips — nothing emitted yet.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings." } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The recipe's IR-slot section, live: channels.telegram lands in IrChannels, and both tokens are {kind: env} refs. The secret name is in the artifact; the secret never is.”", "script": "The terminal is running “Show the lowered IR” now; its output and exit status are the evidence for this beat. [[pause:1s]] The recipe's IR-slot section, live: channels.telegram lands in IrChannels, and both tokens are {kind: env} refs. The secret name is in the artifact; the secret never is.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the 46-line IR for target channel — botToken/secretToken lower to {kind:\"env\"} refs" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no bot token anywhere.”", "script": "The terminal is running “Compile the bundle — and check it” now; its output and exit status are the evidence for this beat. [[pause:1s]] --check asserts the shape, installs the deps, and boots it: GREEN, with no bot token anywhere.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated (boot reached its spec env refs gate)", "produces": ["live/dist/daemon.ts", "live/dist/gateway.ts", "live/dist/session-router.ts"] } },
        { "id": "prove-open-daemon", "label": "Open the emitted daemon", "cue": "“Adapter, gateway, session router, janitor, SIGTERM handler — out of twenty lines of YAML.”", "script": "The editor is opening the generated daemon for “Open the emitted daemon,” so we can inspect the concrete artifact on screen. [[pause:1s]] Adapter, gateway, session router, janitor, SIGTERM handler — out of twenty lines of YAML.", "action": "open", "file": "live/dist/daemon.ts" },
        { "id": "prove-secret", "label": "Find Step 3's authentication", "cue": "“Telegram signs nothing. The whole check is one header compared with timingSafeEqual — in the adapter's own words.”", "script": "The terminal is running “Find Step 3's authentication” now; its output and exit status are the evidence for this beat. [[pause:1s]] Telegram signs nothing. The whole check is one header compared with timingSafeEqual — in the adapter's own words.", "action": "command", "command": "grep -rn \"timingSafeEqual\\|X-Telegram-Bot-Api-Secret-Token\" live/dist/node_modules/@crewhaus/channel-adapter-telegram/dist", "verify": { "mode": "offline", "expectedExit": 0, "note": "6 lines across verify.js/verify.d.ts/index.js — the secret-token comparison is timingSafeEqual, exactly as Step 3 claims" } },
        { "id": "prove-dedup", "label": "Find the idempotency guard", "cue": "“Telegram retries anything that doesn't answer in ten seconds. The gateway remembers the update_id and answers the retry with a 200 duplicate.”", "script": "The terminal is running “Find the idempotency guard” now; its output and exit status are the evidence for this beat. [[pause:1s]] Telegram retries anything that doesn't answer in ten seconds. The gateway remembers the update_id and answers the retry with a 200 duplicate.", "action": "command", "command": "grep -n \"invalid signature\\|remember(\\|duplicate\" live/dist/gateway.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "4 lines — 401 on a bad secret token on both routes, then the atomic idempotency-key dedup that makes a retry a no-op" } }
      ]
    },
    {
      "title": "Beat 3 · Step 4 — run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL. A channel is a daemon — and the CLI names the shapes it takes instead of guessing.”", "script": "This command intentionally exits 1; the nonzero status is the security or validation proof, not a failed demo. [[pause:1s]] run is the cli/browser REPL. A channel is a daemon — and the CLI names the shapes it takes instead of guessing.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection — `crewhaus run supports target: cli or browser (got \"channel\")`" } },
        { "id": "run-gate", "label": "Boot the daemon with no secrets", "cue": "“It names every variable it needs and exits 2 — before it opens a socket.”", "script": "This command intentionally exits 2; the nonzero status is the security or validation proof, not a failed demo. [[pause:1s]] It names every variable it needs and exits 2 — before it opens a socket.", "action": "command", "command": "env -u TELEGRAM_BOT_TOKEN -u TELEGRAM_SECRET_TOKEN -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_API_KEY bun live/dist/daemon.ts", "verify": { "mode": "offline", "expectedExit": 2, "note": "designed refusal — `[daemon] missing required env vars: TELEGRAM_BOT_TOKEN, TELEGRAM_SECRET_TOKEN, ANTHROPIC_AUTH_TOKEN or ANTHROPIC_API_KEY`; env -u scrubs what Setup sourced so the gate fires either way" } },
        { "id": "run-tunnel", "label": "Give the webhook a public URL", "cue": "[manual] The recipe's `ngrok http 3000` — Telegram only POSTs to a public HTTPS host, and this daemon is webhook-only.", "script": "This is an operator-controlled step: “Give the webhook a public URL.” It depends on external state that the offline walkthrough cannot safely supply. [[pause:1.5s]] The recipe's `ngrok http 3000` — Telegram only POSTs to a public HTTPS host, and this daemon is webhook-only.", "action": "command", "command": "ngrok http 3000", "verify": { "mode": "manual", "note": "needs ngrok (or any tunnel) installed and a second terminal; there is no polling path in the emitted daemon" } },
        { "id": "run-daemon", "label": "Boot the daemon for real", "cue": "[needs-key] “Two tokens and an API key: it's listening on :3000 for /telegram/events.”", "script": "The terminal is starting the credentialed step “Boot the daemon for real” now; it may make a real provider or service call. [[pause:1.2s]] Two tokens and an API key: it's listening on :3000 for /telegram/events.", "action": "command", "command": "bun live/dist/daemon.ts", "verify": { "mode": "needs-key", "note": "needs TELEGRAM_BOT_TOKEN + TELEGRAM_SECRET_TOKEN + Anthropic credentials; serves the gateway on PORT (default 3000)" } }
      ]
    },
    {
      "title": "One more thing · Step 4's setWebhook, without the curl",
      "beats": [
        { "id": "extra-provision-dry", "label": "Print the setWebhook call instead of sending it", "cue": "“The recipe's four-line curl, as one command: same URL, same secret_token, same allowed_updates filter — printed, not sent.”", "script": "The terminal is running “Print the setWebhook call instead of sending it” now; its output and exit status are the evidence for this beat. [[pause:1s]] The recipe's four-line curl, as one command: same URL, same secret_token, same allowed_updates filter — printed, not sent.", "action": "command", "command": "crewhaus channel provision live/crewhaus.yaml --platform telegram --base-url https://bot.example.com --dry-run", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the POST to api.telegram.org/bot$TELEGRAM_BOT_TOKEN/setWebhook with url https://bot.example.com/telegram/events and allowed_updates [message, edited_message, callback_query]; sends nothing" } },
        { "id": "extra-smoke", "label": "Run the recipe's smoke", "cue": "“The four things the recipe says it validates: a signed synthetic payload, parseInbound's fields, sendReply's URL, and a retried update_id.”", "script": "The terminal is running “Run the recipe's smoke” now; its output and exit status are the evidence for this beat. [[pause:1s]] The four things the recipe says it validates: a signed synthetic payload, parseInbound's fields, sendReply's URL, and a retried update_id.", "action": "command", "command": "bun ../../../smoke/section-33-telegram-smoke/smoke.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "all probes pass with no key — probe F (live sendMessage) self-skips without TELEGRAM_BOT_TOKEN + CREWHAUS_TELEGRAM_LIVE_CHAT_ID" } }
      ]
    }
  ]
}
