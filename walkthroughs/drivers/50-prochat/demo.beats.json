{
  "title": "Recipe 50 — Pro-grade Chat (à la ChatGPT): tools, routing, and a research sub-agent",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“The assistant shape: browse, run code, look at an image — all declared, none of it bolted on at runtime.”", "script": "We’re opening the full walkthrough before changing anything, keeping the explanation and the implementation side by side. [[pause:1s]] The assistant shape: browse, run code, look at an image — all declared, none of it bolted on at runtime.", "action": "open", "file": "../../50-prochat.md", "preview": true },
        { "id": "setup-reset", "label": "Blank the scratch file", "cue": "live/ is scratch — the committed showcase is never touched by a take.", "script": "We’re preparing a clean scratch state for “Blank the scratch file,” so this section starts from a repeatable baseline. [[pause:800ms]] live/ is scratch — the committed showcase is never touched by a take. The committed starter and walkthrough stay unchanged, which makes another take reproducible.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — Step 1 is run it first.", "script": "We’re loading the demo environment into this terminal session; credentials remain in the shell and are never written into the spec. [[pause:1.2s]] Sources demos/.env — Step 1 is run it first. Later credentialed beats reuse this exact shell environment without persisting the secret in the manifest.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · Step 1 — the committed spec",
      "beats": [
        { "id": "spec-open", "label": "Open it", "cue": "“A hundred and seventy-six lines for a full assistant. Read the tools block first — that's the whole capability surface.”", "script": "The editor is opening the scratch CrewHaus spec for “Open it,” so we can inspect the concrete artifact on screen. [[pause:1s]] A hundred and seventy-six lines for a full assistant. Read the tools block first — that's the whole capability surface.", "action": "open", "file": "../../../starters/showcases/prochat/crewhaus.yaml" },
        { "id": "spec-lint", "label": "Lint it in place", "cue": "“Clean. Nothing about a browse tool or a Python sandbox is special to the schema.”", "script": "The terminal is running “Lint it in place” now; its output and exit status are the evidence for this beat. [[pause:1s]] Clean. Nothing about a browse tool or a Python sandbox is special to the schema.", "action": "command", "command": "crewhaus lint ../../../starters/showcases/prochat/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "spec-check", "label": "Compile it and check it", "cue": "“--check: GREEN, gated at the model credential.”", "script": "The terminal is running “Compile it and check it” now; its output and exit status are the evidence for this beat. [[pause:1s]] --check: GREEN, gated at the model credential. Expected terminal result: GREEN — assertion ok; install ok; boot gated at the provider credentials gate.", "action": "command", "command": "crewhaus compile ../../../starters/showcases/prochat/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok; install ok; boot gated at the provider credentials gate", "produces": ["live/dist/agent.ts"] } }
      ]
    },
    {
      "title": "Beat 2 · Steps 2–4 — the sections",
      "beats": [
        { "id": "sec-tools", "label": "Type the tools block", "cue": "“Step 2: this is the whole capability surface. If it isn't here, the agent cannot do it — there is no runtime plugin path.”", "script": "The driver is copying the exact source-backed lines for “Type the tools block” into the scratch artifact now. [[pause:1.2s]] Step 2: this is the whole capability surface. If it isn't here, the agent cannot do it — there is no runtime plugin path.", "action": "type", "source": "../../50-prochat.md", "sourceLines": [98, 105], "target": "live/tools.yaml", "replace": true },
        { "id": "sec-open-tools", "label": "Read it", "cue": "“Declared capability, not discovered capability.”", "script": "The editor is opening the tools configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] Declared capability, not discovered capability. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "live/tools.yaml" },
        { "id": "sec-perm", "label": "Type the Python allow rule", "cue": "“Step 2, continued: Python runs sandboxed, so it's alwaysAllow. The sandbox is what earns the rule — not trust.”", "script": "The driver is copying the exact source-backed lines for “Type the Python allow rule” into the scratch artifact now. [[pause:1.2s]] Step 2, continued: Python runs sandboxed, so it's alwaysAllow. The sandbox is what earns the rule — not trust.", "action": "type", "source": "../../50-prochat.md", "sourceLines": [115, 117], "target": "live/permissions.yaml", "replace": true },
        { "id": "sec-subagents", "label": "Type the web-researcher sub-agent", "cue": "“Step 3: research fans out into a child with its own context, so a ten-page read doesn't evict your conversation.”", "script": "The driver is copying the exact source-backed lines for “Type the web-researcher sub-agent” into the scratch artifact now. [[pause:1.2s]] Step 3: research fans out into a child with its own context, so a ten-page read doesn't evict your conversation.", "action": "type", "source": "../../50-prochat.md", "sourceLines": [126, 139], "target": "live/sub-agents.yaml", "replace": true },
        { "id": "sec-open-subagents", "label": "Read it", "cue": "“The child reports a summary back. The parent never sees the ten pages.”", "script": "The editor is opening the sub agents configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] The child reports a summary back. The parent never sees the ten pages.", "action": "open", "file": "live/sub-agents.yaml" },
        { "id": "sec-grep", "label": "Find them in the bundle", "cue": "“Tools, rules, and the sub-agent — all present as generated code.”", "script": "The terminal is running “Find them in the bundle” now; its output and exit status are the evidence for this beat. [[pause:1s]] Tools, rules, and the sub-agent — all present as generated code. Expected terminal result: the declared surface, as emitted code.", "action": "command", "command": "grep -inE 'subAgent|permission|tool' live/dist/agent.ts > live/wired.txt", "verify": { "mode": "offline", "expectedExit": 0, "note": "the declared surface, as emitted code", "produces": ["live/wired.txt"] } },
        { "id": "sec-open-wired", "label": "Read what got wired", "cue": "“Every capability traceable from one YAML line to one emitted line.”", "script": "The editor is opening the wired artifact for “Read what got wired,” so we can inspect the concrete artifact on screen. [[pause:1s]] Every capability traceable from one YAML line to one emitted line. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "live/wired.txt" }
      ]
    },
    {
      "title": "Beat 3 · Step 1, for real",
      "beats": [
        { "id": "run-repl", "label": "Launch it", "cue": "[needs-key] “The recipe's own five questions, in order — each one exercises a different tool.”", "script": "The terminal is starting the credentialed step “Launch it” now; it may make a real provider or service call. [[pause:1.2s]] The recipe's own five questions, in order — each one exercises a different tool.", "action": "command", "command": "crewhaus run ../../../starters/showcases/prochat/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "interactive REPL; needs a live model credential" } },
        { "id": "run-plain", "label": "Ask something it just knows", "cue": "[needs-key] “No tool call at all — a good assistant doesn't reach for a browser to answer this.”", "script": "We’re sending the next prompt for “Ask something it just knows” to the process already running in the demo terminal. [[pause:1.2s]] No tool call at all — a good assistant doesn't reach for a browser to answer this.", "action": "input", "text": "what's the capital of Burkina Faso?", "verify": { "mode": "needs-key" } },
        { "id": "run-code", "label": "Ask it to run code", "cue": "[needs-key] “Step 2's sandboxed Python — a chart comes back, and nothing ran on your machine.”", "script": "We’re sending the next prompt for “Ask it to run code” to the process already running in the demo terminal. [[pause:1.2s]] Step 2's sandboxed Python — a chart comes back, and nothing ran on your machine.", "action": "input", "text": "/code plot the first 30 Fibonacci numbers on a log axis", "verify": { "mode": "needs-key" } },
        { "id": "run-math", "label": "Ask it to show its work", "cue": "[needs-key] “17! / 12! — watch whether it reasons or reaches for the sandbox. Both are defensible; the point is you can see which.”", "script": "We’re sending the next prompt for “Ask it to show its work” to the process already running in the demo terminal. [[pause:1.2s]] 17! / 12! — watch whether it reasons or reaches for the sandbox. Both are defensible; the point is you can see which.", "action": "input", "text": "what's 17! / 12!?  show your work", "verify": { "mode": "needs-key" } },
        { "id": "run-exit", "label": "Exit", "cue": "[needs-key] “exit” returns to the shell.", "script": "We’re sending exit to the active REPL, closing the interactive process without discarding the terminal transcript. [[pause:1.2s]] “exit” returns to the shell. The completed interaction stays visible in the terminal for review.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · Step 6 — swap the model",
      "beats": [
        { "id": "swap-grep", "label": "Find the model line", "cue": "“One line. Everything else in the spec is provider-agnostic.”", "script": "The terminal is running “Find the model line” now; its output and exit status are the evidence for this beat. [[pause:1s]] One line. Everything else in the spec is provider-agnostic. Expected terminal result: the single provider-bound line.", "action": "command", "command": "grep -n 'model:' ../../../starters/showcases/prochat/crewhaus.yaml > live/model.txt", "verify": { "mode": "offline", "expectedExit": 0, "note": "the single provider-bound line", "produces": ["live/model.txt"] } },
        { "id": "swap-open", "label": "Read it", "cue": "“A cheaper default is a defensible default for chat — the recipe says so, and says why.”", "script": "The editor is opening the model artifact for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] A cheaper default is a defensible default for chat — the recipe says so, and says why.", "action": "open", "file": "live/model.txt" },
        { "id": "swap-table", "label": "Read the provider table", "cue": "“OpenAI, Gemini, Bedrock, local — same spec, different prefix.”", "script": "The editor is opening the walkthrough for “Read the provider table,” so we can inspect the concrete artifact on screen. [[pause:1s]] OpenAI, Gemini, Bedrock, local — same spec, different prefix. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "../../50-prochat.md", "preview": true }
      ]
    }
  ]
}
