{
  "title": "Recipe 13 — MCP Servers (tools you didn't write, namespaced and gated)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“MCP is how you get tools you didn't write — a community filesystem server, a Postgres server, someone else's Go binary.”", "script": "MCP is how you get tools you didn't write — a community filesystem server, a Postgres server, someone else's Go binary. [[pause:1.2s]] The editor opens 13 mcp servers.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "../../13-mcp-servers.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed recipe is never touched by a take.", "script": "Live/ is scratch — the committed recipe is never touched by a take. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — the run beat calls a remote tool, which means calling the model first.", "script": "Sources demos/.env — the run beat calls a remote tool, which means calling the model first. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · Adding an MCP server",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“A plain cli agent — MCP adds tools to it, it doesn't change the shape.”", "script": "A plain cli agent — MCP adds tools to it, it doesn't change the shape. [[pause:900ms]] Watch the driver type lines 51 through 52 of 13 mcp servers.md, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [51, 52], "replace": true },
        { "id": "spec-agent", "label": "Add the agent block", "cue": "“The instructions name the prefix, because the prefix is what the model will see.”", "script": "The instructions name the prefix, because the prefix is what the model will see. [[pause:900ms]] Watch lines 53 through 57 of 13 mcp servers.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [53, 57] },
        { "id": "spec-mcp", "label": "Wire the reference MCP server", "cue": "“Five lines: a name, a transport, and the command the runtime spawns. That's the whole integration.”", "script": "Five lines: a name, a transport, and the command the runtime spawns. That's the whole integration. [[pause:900ms]] Watch lines 58 through 62 of 13 mcp servers.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [58, 62] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing spawned, nothing emitted.”", "script": "Parse plus the IR passes the compile path skips — nothing spawned, nothing emitted. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“mcpServers survives lowering verbatim — transport, command, args. The compiler doesn't interpret them; the runtime does.”", "script": "McpServers survives lowering verbatim — transport, command, args. The compiler doesn't interpret them; the runtime does. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target cli, mcpServers.everything included.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target cli, mcpServers.everything included" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, no key.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, no key. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (11 anchors); install ok; boot gated at the provider credentials gate, which is reached before the MCP handshake.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (11 anchors); install ok; boot gated at the provider credentials gate, which is reached before the MCP handshake", "produces": ["live/dist/agent.ts", "live/dist/package.json"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "“The MCP host is a runtime import, not generated code — the bundle hands it the server table and gets a tool list back.”", "script": "The MCP host is a runtime import, not generated code — the bundle hands it the server table and gets a tool list back. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Call a tool you didn't write",
      "beats": [
        { "id": "run-repl", "label": "Launch the REPL with MCP debug on", "cue": "[needs-key] “CREWHAUS_MCP_DEBUG=1 logs every MCP message — the handshake, tools/list, tools/call.”", "script": "CREWHAUS_MCP_DEBUG=1 logs every MCP message — the handshake, tools/list, tools/call. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Spawns npx @modelcontextprotocol/server-everything on the way up; the chat loop needs Anthropic credentials.", "action": "command", "command": "CREWHAUS_MCP_DEBUG=1 crewhaus run live/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "spawns npx @modelcontextprotocol/server-everything on the way up; the chat loop needs Anthropic credentials" } },
        { "id": "run-echo", "label": "Ask it to use the remote tool", "cue": "[needs-key] “The model asks for everything__echo — prefixed, because two servers with a `read` tool would otherwise collide.”", "script": "The model asks for everything__echo — prefixed, because two servers with a `read` tool would otherwise collide. [[pause:1.2s]] The driver sends the next user turn into the active session: “echo hello using the everything server”. Runtime serializes tools/call over stdio; the response is classified with origin \"mcp\" before it reaches the model.", "action": "input", "text": "echo hello using the everything server", "verify": { "mode": "needs-key", "note": "runtime serializes tools/call over stdio; the response is classified with origin \"mcp\" before it reaches the model" } },
        { "id": "run-exit", "label": "Exit the REPL", "cue": "[needs-key] “exit” returns to the shell, and the runtime reaps the server subprocess.", "script": "“exit” returns to the shell, and the runtime reaps the server subprocess. [[pause:1.2s]] The driver sends exit to close the active REPL cleanly. The terminal returns to a normal shell while the generated artifacts and session history remain available.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · many servers, one spec — and permissions on the prefixed names",
      "beats": [
        { "id": "multi-reset", "label": "Blank a second scratch spec", "cue": "“A production CLI agent runs three to five of these. There's no limit and no shared state.”", "script": "A production CLI agent runs three to five of these. There's no limit and no shared state. [[pause:900ms]] The driver clears live slash multi.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/multi.yaml" },
        { "id": "multi-head", "label": "Same agent, second spec", "cue": "“Identical head — the interesting part is what we hang off it.”", "script": "Identical head — the interesting part is what we hang off it. [[pause:900ms]] Watch the driver type lines 51 through 57 of 13 mcp servers.md, rebuilding live slash multi.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [51, 57], "target": "live/multi.yaml", "replace": true },
        { "id": "multi-servers", "label": "Wire four servers, two transports", "cue": "“Two stdio subprocesses the runtime spawns, two SSE connections to servers someone else operates.”", "script": "Two stdio subprocesses the runtime spawns, two SSE connections to servers someone else operates. [[pause:900ms]] Watch lines 217 through 230 of 13 mcp servers.md become the next block of live slash multi.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [217, 230], "target": "live/multi.yaml" },
        { "id": "multi-perms", "label": "Gate the remote tools by name", "cue": "“Permission rules match the prefixed name — same grammar as Read and Bash. read_file allowed, write_file asks, create_repository denied.”", "script": "Permission rules match the prefixed name — same grammar as Read and Bash. read_file allowed, write_file asks, create_repository denied. [[pause:900ms]] Watch lines 194 through 201 of 13 mcp servers.md become the next block of live slash multi.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "../../13-mcp-servers.md", "sourceLines": [194, 201], "target": "live/multi.yaml" },
        { "id": "multi-lint", "label": "Lint the four-server spec", "cue": "“Clean — and note lint never dials the SSE URLs. Nothing here needs those services to exist.”", "script": "Clean — and note lint never dials the SSE URLs. Nothing here needs those services to exist. [[pause:1.5s]] This is an offline proof and should complete successfully. Lint: clean — no findings; sse urls are not resolved at lint time.", "action": "command", "command": "crewhaus lint live/multi.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings; sse urls are not resolved at lint time" } },
        { "id": "multi-ir", "label": "Lower it to IR on disk", "cue": "“Four servers, and the three permission rules beside them — one JSON object the runtime consumes.”", "script": "Four servers, and the three permission rules beside them — one JSON object the runtime consumes. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Redirect, not a pipe — the exit code is the compiler's.", "action": "command", "command": "crewhaus compile live/multi.yaml --emit-ir > live/multi.ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "redirect, not a pipe — the exit code is the compiler's", "produces": ["live/multi.ir.json"] } },
        { "id": "multi-open", "label": "Read the lowered server table", "cue": "“filesystem, git, postgres, sentry — and the model will see filesystem__read_file, github__create_issue, and so on.”", "script": "Filesystem, git, postgres, sentry — and the model will see filesystem__read_file, github__create_issue, and so on. [[pause:1.2s]] The editor opens live slash multi.ir.json now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "live/multi.ir.json" }
      ]
    }
  ]
}
