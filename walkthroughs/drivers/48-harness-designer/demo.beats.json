{
  "title": "Recipe 48 — Harness Designer (the meta-recipe: an agent that writes specs)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“The compiler's own dogfood: a harness whose job is designing harnesses.”", "script": "We’re opening the full walkthrough before changing anything, keeping the explanation and the implementation side by side. [[pause:1s]] The compiler's own dogfood: a harness whose job is designing harnesses.", "action": "open", "file": "../../48-harness-designer.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed designer is never touched by a take.", "script": "We’re preparing a clean scratch state for “Start from a blank spec,” so this section starts from a repeatable baseline. [[pause:800ms]] live/ is scratch — the committed designer is never touched by a take. The committed starter and walkthrough stay unchanged, which makes another take reproducible.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env — the designer interviews you on a live model.", "script": "We’re loading the demo environment into this terminal session; credentials remain in the shell and are never written into the spec. [[pause:1.2s]] Sources demos/.env — the designer interviews you on a live model. Later credentialed beats reuse this exact shell environment without persisting the secret in the manifest.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · Step 1 — the designer itself",
      "beats": [
        { "id": "spec-open", "label": "Open the committed designer", "cue": "“Three hundred lines, and every one of them is instructions. The designer has no special compiler privileges.”", "script": "The editor is opening the scratch CrewHaus spec for “Open the committed designer,” so we can inspect the concrete artifact on screen. [[pause:1s]] Three hundred lines, and every one of them is instructions. The designer has no special compiler privileges.", "action": "open", "file": "../../../starters/harness-designer/crewhaus.yaml" },
        { "id": "spec-lint", "label": "Lint it in place", "cue": "“Same lint as every other spec. The thing that writes specs is itself just a spec.”", "script": "The terminal is running “Lint it in place” now; its output and exit status are the evidence for this beat. [[pause:1s]] Same lint as every other spec. The thing that writes specs is itself just a spec.", "action": "command", "command": "crewhaus lint ../../../starters/harness-designer/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } }
      ]
    },
    {
      "title": "Beat 2 · Step 2 — compile, don't trust",
      "beats": [
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“One agent, one chat loop. All the intelligence is in the prompt, which is exactly the point.”", "script": "The terminal is running “Show the lowered IR” now; its output and exit status are the evidence for this beat. [[pause:1s]] One agent, one chat loop. All the intelligence is in the prompt, which is exactly the point.", "action": "command", "command": "crewhaus compile ../../../starters/harness-designer/crewhaus.yaml --emit-ir > live/ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "IR written to live/ir.json", "produces": ["live/ir.json"] } },
        { "id": "prove-open-ir", "label": "Open the IR", "cue": "“Nothing exotic. The designer earns its output the same way any agent does.”", "script": "The editor is opening the lowered intermediate representation for “Open the IR,” so we can inspect the concrete artifact on screen. [[pause:1s]] Nothing exotic. The designer earns its output the same way any agent does.", "action": "open", "file": "live/ir.json" },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check: GREEN, gated at the model credential. The designer is a shippable bundle like everything else.”", "script": "The terminal is running “Compile the bundle — and check it” now; its output and exit status are the evidence for this beat. [[pause:1s]] --check: GREEN, gated at the model credential. The designer is a shippable bundle like everything else.", "action": "command", "command": "crewhaus compile ../../../starters/harness-designer/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok; install ok; boot gated at the provider credentials gate", "produces": ["live/dist/agent.ts"] } }
      ]
    },
    {
      "title": "Beat 3 · Step 4 — why validation matters",
      "beats": [
        { "id": "valid-static", "label": "Run the static recipe gate", "cue": "“The designer's own claim is that its output compiles. This is the gate that would catch it lying — every spec fence in every recipe, compiled.”", "script": "The terminal is running “Run the static recipe gate” now; its output and exit status are the evidence for this beat. [[pause:1s]] The designer's own claim is that its output compiles. This is the gate that would catch it lying — every spec fence in every recipe, compiled.", "action": "command", "command": "bun ../../../scripts/test-walkthroughs.ts > live/gate.txt 2>&1", "verify": { "mode": "offline", "expectedExit": 0, "note": "every recipe validated: links resolve, spec fences compile, bun scripts exist", "produces": ["live/gate.txt"] } },
        { "id": "valid-open", "label": "Read the result", "cue": "“A designer that emits specs is only as trustworthy as the gate that checks them.”", "script": "The editor is opening the gate artifact for “Read the result,” so we can inspect the concrete artifact on screen. [[pause:1s]] A designer that emits specs is only as trustworthy as the gate that checks them.", "action": "open", "file": "live/gate.txt" }
      ]
    },
    {
      "title": "One more thing · interview it",
      "beats": [
        { "id": "run-designer", "label": "Launch the designer", "cue": "[needs-key] “It opens by asking what you want to build. Answer in a sentence.”", "script": "The terminal is starting the credentialed step “Launch the designer” now; it may make a real provider or service call. [[pause:1.2s]] It opens by asking what you want to build. Answer in a sentence. Operational note: interactive REPL; needs a live model credential.", "action": "command", "command": "crewhaus run ../../../starters/harness-designer/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "interactive REPL; needs a live model credential" } },
        { "id": "run-ask", "label": "Describe a harness", "cue": "[needs-key] “Watch it pick a target shape and justify the choice before writing a line of YAML.”", "script": "We’re sending the next prompt for “Describe a harness” to the process already running in the demo terminal. [[pause:1.2s]] Watch it pick a target shape and justify the choice before writing a line of YAML.", "action": "input", "text": "I want an agent that reads our support inbox and drafts replies for a human to approve.", "verify": { "mode": "needs-key" } },
        { "id": "run-refine", "label": "Push back on it", "cue": "[needs-key] “Step 3's worked dialogues: disagreeing with it is part of the design loop.”", "script": "We’re sending the next prompt for “Push back on it” to the process already running in the demo terminal. [[pause:1.2s]] Step 3's worked dialogues: disagreeing with it is part of the design loop.", "action": "input", "text": "Approvals have to be durable — a reviewer might come back a day later. Does that change the shape?", "verify": { "mode": "needs-key" } },
        { "id": "run-exit", "label": "Exit", "cue": "[needs-key] “exit” returns to the shell.", "script": "We’re sending exit to the active REPL, closing the interactive process without discarding the terminal transcript. [[pause:1.2s]] “exit” returns to the shell. The completed interaction stays visible in the terminal for review.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } },
        { "id": "extra-optimize", "label": "Read the optimize handoff", "cue": "“Step 5: the spec the designer hands you is the input to recipe 42's loop. Design, then optimize.”", "script": "The editor is opening the walkthrough for “Read the optimize handoff,” so we can inspect the concrete artifact on screen. [[pause:1s]] Step 5: the spec the designer hands you is the input to recipe 42's loop. Design, then optimize.", "action": "open", "file": "../../48-harness-designer.md", "preview": true }
      ]
    }
  ]
}
