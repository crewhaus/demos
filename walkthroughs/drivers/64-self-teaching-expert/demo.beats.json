{
  "title": "Recipe 64 — The self-teaching expert (memory, curriculum, and a living exam)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“Most agents forget everything between sessions. This one keeps a curriculum and grades itself against it.”", "script": "We’re opening the full walkthrough before changing anything, keeping the explanation and the implementation side by side. [[pause:1s]] Most agents forget everything between sessions. This one keeps a curriculum and grades itself against it.", "action": "open", "file": "../../64-self-teaching-expert.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed expert is never touched by a take.", "script": "We’re preparing a clean scratch state for “Start from a blank spec,” so this section starts from a repeatable baseline. [[pause:800ms]] live/ is scratch — the committed expert is never touched by a take. The committed starter and walkthrough stay unchanged, which makes another take reproducible.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the credentials", "cue": "[needs-key] Sources demos/.env — THREDZ_API_KEY for the hosted wiki, plus the model credential.", "script": "We’re loading the demo environment into this terminal session; credentials remain in the shell and are never written into the spec. [[pause:1.2s]] Sources demos/.env — THREDZ_API_KEY for the hosted wiki, plus the model credential.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · the expert harness",
      "beats": [
        { "id": "spec-open", "label": "Open the committed expert", "cue": "“Memory, a wiki store, and a heartbeat. The heartbeat is what makes it study when nobody's asking.”", "script": "The editor is opening the scratch CrewHaus spec for “Open the committed expert,” so we can inspect the concrete artifact on screen. [[pause:1s]] Memory, a wiki store, and a heartbeat. The heartbeat is what makes it study when nobody's asking.", "action": "open", "file": "../../../starters/expert/crewhaus.yaml" },
        { "id": "spec-lint", "label": "Lint it in place", "cue": "“Clean — the whole learning loop is declared, not scripted.”", "script": "The terminal is running “Lint it in place” now; its output and exit status are the evidence for this beat. [[pause:1s]] Clean — the whole learning loop is declared, not scripted. Expected terminal result: lint: clean — no findings.", "action": "command", "command": "crewhaus lint ../../../starters/expert/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "spec-thredz", "label": "Type the wiki-store line", "cue": "“`thredz: true` is shorthand for the hosted store. One line, and the agent has somewhere durable to write what it learns.”", "script": "The driver is copying the exact source-backed lines for “Type the wiki-store line” into the scratch artifact now. [[pause:1.2s]] `thredz: true` is shorthand for the hosted store. One line, and the agent has somewhere durable to write what it learns.", "action": "type", "source": "../../64-self-teaching-expert.md", "sourceLines": [32, 38], "target": "live/store.yaml", "replace": true },
        { "id": "spec-open-thredz", "label": "Read it", "cue": "“Swap it for a local path and nothing else in the spec changes.”", "script": "The editor is opening the store configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] Swap it for a local path and nothing else in the spec changes. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "live/store.yaml" }
      ]
    },
    {
      "title": "Beat 2 · compile, don't trust",
      "beats": [
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The curriculum and the wiki are IR nodes. Learning is part of the shape, not a library the agent imports.”", "script": "The terminal is running “Show the lowered IR” now; its output and exit status are the evidence for this beat. [[pause:1s]] The curriculum and the wiki are IR nodes. Learning is part of the shape, not a library the agent imports.", "action": "command", "command": "crewhaus compile ../../../starters/expert/crewhaus.yaml --emit-ir > live/ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "IR written to live/ir.json", "produces": ["live/ir.json"] } },
        { "id": "prove-open-ir", "label": "Open the IR", "cue": "“Read the memory and knowledge nodes — that's the whole studying apparatus.”", "script": "The editor is opening the lowered intermediate representation for “Open the IR,” so we can inspect the concrete artifact on screen. [[pause:1s]] Read the memory and knowledge nodes — that's the whole studying apparatus.", "action": "open", "file": "live/ir.json" },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check: GREEN, and the gate it stops at is the MCP server credential the spec itself declares. A named gate, not a failure.”", "script": "The terminal is running “Compile the bundle — and check it” now; its output and exit status are the evidence for this beat. [[pause:1s]] --check: GREEN, and the gate it stops at is the MCP server credential the spec itself declares. A named gate, not a failure.", "action": "command", "command": "crewhaus compile ../../../starters/expert/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — boot gated (boot reached its MCP server credentials gate: THREDZ_API_KEY)", "produces": ["live/dist/agent.ts"] } },
        { "id": "prove-heartbeat", "label": "Type the heartbeat block", "cue": "“study/reflect on a schedule. This is what turns a chat agent into something that gets better while you sleep.”", "script": "The driver is copying the exact source-backed lines for “Type the heartbeat block” into the scratch artifact now. [[pause:1.2s]] study/reflect on a schedule. This is what turns a chat agent into something that gets better while you sleep.", "action": "type", "source": "../../64-self-teaching-expert.md", "sourceLines": [231, 235], "target": "live/heartbeat.yaml", "replace": true },
        { "id": "prove-open-hb", "label": "Read it", "cue": "“An interval and a task. Cheap to declare, and the entire difference between static and self-teaching.”", "script": "The editor is opening the heartbeat configuration for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] An interval and a task. Cheap to declare, and the entire difference between static and self-teaching.", "action": "open", "file": "live/heartbeat.yaml" }
      ]
    },
    {
      "title": "Beat 3 · the living exam",
      "beats": [
        { "id": "exam-datasets", "label": "Look for the exam", "cue": "“The 'living exam' is a registered dataset — empty here, because it's earned from real sessions.”", "script": "The terminal is running “Look for the exam” now; its output and exit status are the evidence for this beat. [[pause:1s]] The 'living exam' is a registered dataset — empty here, because it's earned from real sessions.", "action": "command", "command": "crewhaus datasets list > live/datasets.txt 2>&1", "verify": { "mode": "offline", "expectedExit": 0, "note": "no datasets registered yet — the honest starting state", "produces": ["live/datasets.txt"] } },
        { "id": "exam-open", "label": "Read it", "cue": "“You can't grade an expert against an exam it hasn't sat yet.”", "script": "The editor is opening the datasets artifact for “Read it,” so we can inspect the concrete artifact on screen. [[pause:1s]] You can't grade an expert against an exam it hasn't sat yet. Read the file on screen as the concrete output or contract behind the preceding step.", "action": "open", "file": "live/datasets.txt" }
      ]
    },
    {
      "title": "One more thing · teach it something",
      "beats": [
        { "id": "run-expert", "label": "Run the expert", "cue": "[needs-key] “Ask it something outside what it knows, and watch it log the gap rather than bluff.”", "script": "The terminal is starting the credentialed step “Run the expert” now; it may make a real provider or service call. [[pause:1.2s]] Ask it something outside what it knows, and watch it log the gap rather than bluff.", "action": "command", "command": "crewhaus run ../../../starters/expert/crewhaus.yaml", "verify": { "mode": "needs-key", "note": "needs THREDZ_API_KEY and a model credential" } },
        { "id": "run-ask", "label": "Ask past the edge of its knowledge", "cue": "[needs-key] “The logged gap becomes tomorrow's curriculum entry. That's the loop.”", "script": "We’re sending the next prompt for “Ask past the edge of its knowledge” to the process already running in the demo terminal. [[pause:1.2s]] The logged gap becomes tomorrow's curriculum entry. That's the loop.", "action": "input", "text": "What changed in our refund policy last quarter?", "verify": { "mode": "needs-key" } },
        { "id": "run-exit", "label": "Exit", "cue": "[needs-key] “exit” returns to the shell.", "script": "We’re sending exit to the active REPL, closing the interactive process without discarding the terminal transcript. [[pause:1.2s]] “exit” returns to the shell. The completed interaction stays visible in the terminal for review.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } },
        { "id": "run-flywheel", "label": "Turn the flywheel on what it learned", "cue": "[needs-key] “learn what to learn → study → get examined → optimize. Recipe 56's loop, pointed at a curriculum.”", "script": "The terminal is starting the credentialed step “Turn the flywheel on what it learned” now; it may make a real provider or service call. [[pause:1.2s]] learn what to learn → study → get examined → optimize. Recipe 56's loop, pointed at a curriculum.", "action": "command", "command": "crewhaus flywheel run --dataset registry:hello-expert-ratings", "verify": { "mode": "needs-key", "note": "needs a registered dataset and a model credential; billed" } }
      ]
    }
  ]
}
