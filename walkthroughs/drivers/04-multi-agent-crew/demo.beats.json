{
  "title": "Recipe 04 — Multi-Agent Crew (three roles, one baton, one trace)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“Recipe 04 opens by telling you not to reach for it: multi-agent helps on parallelizable work and hurts sequential reasoning. Crew is for roles that negotiate.”", "script": "Recipe 04 opens by telling you not to reach for it: multi-agent helps on parallelizable work and hurts sequential reasoning. Crew is for roles that negotiate. [[pause:1.2s]] The editor opens 04 multi agent crew.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "../../04-multi-agent-crew.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed recipe is never touched by a take.", "script": "Live/ is scratch — the committed recipe is never touched by a take. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the boot beats.", "script": "Sources demos/.env for the boot beats. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The smallest spec — three roles, one entry",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape, pick who starts", "cue": "“entry: picks which role takes the first message — the ‘who goes first' problem is settled in the spec, not at runtime. And permissions are crew-wide, not per role.”", "script": "Entry: picks which role takes the first message — the ‘who goes first’ problem is settled in the spec, not at runtime. And permissions are crew-wide, not per role. [[pause:900ms]] Watch the driver type lines 86 through 91 of 04 multi agent crew.md, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [86, 91], "replace": true },
        { "id": "spec-researcher", "label": "Role 1 · the researcher", "cue": "“List 3 concise risks of the topic. Then call Handoff(target=\"writer\") and end your turn.” It hands off; it never writes the post.", "script": "“List 3 concise risks of the topic. Then call Handoff(target=\"writer\") and end your turn.” It hands off; it never writes the post. [[pause:900ms]] Watch lines 92 through 96 of 04 multi agent crew.md become the next block of live slash crewhaus.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [92, 96] },
        { "id": "spec-writer", "label": "Role 2 · the writer", "cue": "“Write a 4-sentence summary post. If you need a sanity check, use SendMessage(target=\"critic\").” Handoff is one-way; SendMessage is request-reply.", "script": "“Write a 4-sentence summary post. If you need a sanity check, use SendMessage(target=\"critic\").” Handoff is one-way; SendMessage is request-reply. [[pause:900ms]] Watch lines 97 through 102 of 04 multi agent crew.md become the next block of live slash crewhaus.yaml. The manifest points at committed lines directly, keeping the executable demo and its narration in lockstep.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [97, 102] },
        { "id": "spec-critic", "label": "Role 3 · the critic", "cue": "“Give a one-sentence direct answer. Be terse.” The peer that answers questions, never the one that ships.", "script": "“Give a one-sentence direct answer. Be terse.” The peer that answers questions, never the one that ships. [[pause:900ms]] Watch lines 103 through 106 of 04 multi agent crew.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [103, 106] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the crew well-formedness passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the crew well-formedness passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“Three roles, one entry, every role on the inherited sonnet — and look at each role's tools: empty.”", "script": "Three roles, one entry, every role on the inherited sonnet — and look at each role's tools: empty. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Prints the IR for target crew (3 roles, entry researcher); each role carries the inherited claude-sonnet-5 and \"tools\": [].", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir", "verify": { "mode": "offline", "expectedExit": 0, "note": "prints the IR for target crew (3 roles, entry researcher); each role carries the inherited claude-sonnet-5 and \"tools\": []" } },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room.”", "script": "--check asserts the shape, installs the deps, and boots it: GREEN, with no key in the room. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (4 anchors); install ok; boot gated at the daemon's stdin input gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated at the daemon's stdin input gate", "produces": ["live/dist/daemon.ts", "live/dist/orchestrator.ts", "live/dist/agent_researcher.ts", "live/dist/node_modules/@crewhaus/crew-orchestrator/dist/index.js"] } },
        { "id": "prove-open-orchestrator", "label": "Open the emitted orchestrator", "cue": "“One file per role, plus this: three roles registered, one entry set. The baton passing is the runtime's job.”", "script": "One file per role, plus this: three roles registered, one entry set. The baton passing is the runtime's job. [[pause:1.2s]] The editor opens live slash dist slash orchestrator.ts now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/dist/orchestrator.ts" },
        { "id": "prove-injected-tools", "label": "So where do Handoff and SendMessage come from?", "cue": "“The recipe says you never declare them — here is the proof. The orchestrator builds both tools per role and pre-allows both patterns.”", "script": "The recipe says you never declare them — here is the proof. The orchestrator builds both tools per role and pre-allows both patterns. [[pause:1.5s]] The terminal is exercising the real implementation locally here, and the verifier expects a successful exit. The installed crew-orchestrator imports createHandoffTool + createSendMessageA2ATool and pre-allows the Handoff / SendMessage patterns — which is why every role's declared tool list is empty.", "action": "command", "command": "grep -nE 'createHandoffTool|createSendMessageA2ATool|alwaysAllow' live/dist/node_modules/@crewhaus/crew-orchestrator/dist/index.js", "verify": { "mode": "offline", "expectedExit": 0, "note": "the installed crew-orchestrator imports createHandoffTool + createSendMessageA2ATool and pre-allows the Handoff / SendMessage patterns — which is why every role's declared tool list is empty" } }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Ask run to run it", "cue": "“run is the cli/browser REPL. A crew isn't a chat — the CLI names the two shapes it takes and exits 1.”", "script": "Run is the cli/browser REPL. A crew isn't a chat — the CLI names the two shapes it takes and exits 1. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Deliberate rejection, exit 1 — `crewhaus run supports target: cli or browser (got \"crew\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "deliberate rejection, exit 1 — `crewhaus run supports target: cli or browser (got \"crew\")`" } },
        { "id": "run-no-input", "label": "Boot the daemon with nothing to say", "cue": "“The recipe says it in as many words: no input on stdin, no turn. It prints that and exits 2 — designed, not broken.”", "script": "The recipe says it in as many words: no input on stdin, no turn. It prints that and exits 2 — designed, not broken. [[pause:1.5s]] The nonzero result is intentional: this command expects exit code 2. Designed refusal — `[crew] no input on stdin`, exit 2; deps were installed by the --check beat.", "action": "command", "command": "bun live/dist/daemon.ts < /dev/null", "verify": { "mode": "offline", "expectedExit": 2, "note": "designed refusal — `[crew] no input on stdin`, exit 2; deps were installed by the --check beat" } },
        { "id": "run-topic", "label": "Hand the crew a topic", "cue": "[needs-key] “Researcher lists three risks, hands the baton to the writer, the writer pings the critic — three model calls minimum, four if it asks.”", "script": "Researcher lists three risks, hands the baton to the writer, the writer pings the critic — three model calls minimum, four if it asks. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Needs Anthropic credentials. The whole crew runs under one traceId and appends to a single sess_<hex>.jsonl; the recipe's debug command is `jq -c 'select(.kind == \"crew_event\")' \"$SESSION\"`.", "action": "command", "command": "echo \"Topic: rolling out vector-search to production\" | bun live/dist/daemon.ts", "verify": { "mode": "needs-key", "note": "needs Anthropic credentials. The whole crew runs under one traceId and appends to a single sess_<hex>.jsonl; the recipe's debug command is `jq -c 'select(.kind == \"crew_event\")' \"$SESSION\"`" } }
      ]
    },
    {
      "title": "One more thing · Per-role models — the cost lever",
      "beats": [
        { "id": "cheap-head", "label": "Same crew, second spec", "cue": "“Same three roles, same entry — now each role picks its own tier.”", "script": "Same three roles, same entry — now each role picks its own tier. [[pause:900ms]] Watch the driver type lines 86 through 91 of 04 multi agent crew.md, rebuilding live slash cheap.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [86, 91], "target": "live/cheap.yaml", "replace": true },
        { "id": "cheap-roles", "label": "Haiku, opus, haiku", "cue": "“Cheap structural extraction, high-quality synthesis, fast review.” The recipe's line: if every role runs the same opus model, the crew is just a chat with extra orchestration.", "script": "“Cheap structural extraction, high-quality synthesis, fast review.” The recipe's line: if every role runs the same opus model, the crew is just a chat with extra orchestration. [[pause:900ms]] Watch lines 284 through 296 of 04 multi agent crew.md become the next block of live slash cheap.yaml. Only this next block is appended, so the specification grows in the same order the runtime will parse it.", "action": "type", "source": "../../04-multi-agent-crew.md", "sourceLines": [284, 296], "target": "live/cheap.yaml" },
        { "id": "cheap-lint", "label": "Lint the mixed-tier crew", "cue": "“Three models in one spec, still clean.”", "script": "Three models in one spec, still clean. [[pause:1.5s]] This is an offline proof and should complete successfully. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/cheap.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "cheap-compile", "label": "Compile it", "cue": "“One file per role — so a per-role model is one literal per file.”", "script": "One file per role — so a per-role model is one literal per file. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. Plain compile (no --check: the shape is already proved above) — emits daemon, orchestrator and one agent file per role.", "action": "command", "command": "crewhaus compile live/cheap.yaml -o live/dist-cheap", "verify": { "mode": "offline", "expectedExit": 0, "note": "plain compile (no --check: the shape is already proved above) — emits daemon, orchestrator and one agent file per role", "produces": ["live/dist-cheap/agent_researcher.ts", "live/dist-cheap/agent_writer.ts", "live/dist-cheap/agent_critic.ts"] } },
        { "id": "cheap-grep", "label": "Read the tiers off the bundle", "cue": "“Researcher on haiku, writer on opus, critic on haiku — the model-router only loads the adapters these two tiers need.”", "script": "Researcher on haiku, writer on opus, critic on haiku — the model-router only loads the adapters these two tiers need. [[pause:1.5s]] This is an offline proof and should complete successfully. Agent_researcher + agent_critic carry claude-haiku-4-5-20251001, agent_writer carries claude-opus-5.", "action": "command", "command": "grep -n 'model:' live/dist-cheap/agent_researcher.ts live/dist-cheap/agent_writer.ts live/dist-cheap/agent_critic.ts", "verify": { "mode": "offline", "expectedExit": 0, "note": "agent_researcher + agent_critic carry claude-haiku-4-5-20251001, agent_writer carries claude-opus-5" } }
      ]
    }
  ]
}
