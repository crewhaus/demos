{
  "title": "Recipe 06 — RAG Pipeline (retrieval is configuration, not code)",
  "cwd": ".",
  "defaults": {
    "target": "live/crewhaus.yaml",
    "speedMsPerChar": 14,
    "chunk": 1
  },
  "groups": [
    {
      "title": "Setup",
      "beats": [
        { "id": "setup-recipe", "label": "Open the recipe", "cue": "“Recipe 06 is the shape you reach for when the boundary of what the agent knows is the corpus you indexed.”", "script": "Recipe 06 is the shape you reach for when the boundary of what the agent knows is the corpus you indexed. [[pause:1.2s]] The editor opens 06 rag pipeline.md now. Keep this artifact in view: it is the concrete evidence behind the behavior this beat describes.", "action": "open", "file": "../../06-rag-pipeline.md", "preview": true },
        { "id": "setup-reset", "label": "Start from a blank spec", "cue": "live/ is scratch — the committed recipe is never touched by a take.", "script": "Live/ is scratch — the committed recipe is never touched by a take. [[pause:900ms]] The driver clears live slash crewhaus.yaml in the scratch workspace now, giving this take a repeatable starting point without changing the committed example.", "action": "reset", "file": "live/crewhaus.yaml" },
        { "id": "setup-env", "label": "Load the API key", "cue": "[needs-key] Sources demos/.env for the run beats. Note the corpus itself needs no key — mock/det embeds offline.", "script": "Sources demos/.env for the run beats. Note the corpus itself needs no key — mock/det embeds offline. [[pause:1.8s]] Configured provider or platform credentials are required here, so the offline verifier checks the shape but does not execute the call. Operator-supplied demos/.env.", "action": "command", "command": "set -a; . ../../../.env; set +a", "verify": { "mode": "needs-key", "note": "operator-supplied demos/.env" } }
      ]
    },
    {
      "title": "Beat 1 · The spec — four blocks, no code",
      "beats": [
        { "id": "spec-head", "label": "Name it, pick the shape", "cue": "“target: pipeline” — chunk → embed → store → retrieve → answer, as an explicit DAG.", "script": "“target: pipeline” — chunk → embed → store → retrieve → answer, as an explicit DAG. [[pause:900ms]] Watch the driver type lines 87 through 88 of 06 rag pipeline.md, rebuilding live slash crewhaus.yaml from a known state. The slice comes from committed source, so the fresh file exactly matches the example this take is introducing.", "action": "type", "source": "../../06-rag-pipeline.md", "sourceLines": [87, 88], "replace": true },
        { "id": "spec-agent", "label": "Add the agent block", "cue": "“Two non-negotiables in the prompt: call Retrieve first, and refuse what the chunks don't cover.”", "script": "Two non-negotiables in the prompt: call Retrieve first, and refuse what the chunks don't cover. [[pause:900ms]] Watch lines 89 through 94 of 06 rag pipeline.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../06-rag-pipeline.md", "sourceLines": [89, 94] },
        { "id": "spec-retrieve", "label": "Wire the retriever", "cue": "“A deterministic embedder into an in-memory store. Swap either one and only one IR node changes — the prompt above is untouched.”", "script": "A deterministic embedder into an in-memory store. Swap either one and only one IR node changes — the prompt above is untouched. [[pause:900ms]] Watch lines 95 through 98 of 06 rag pipeline.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../06-rag-pipeline.md", "sourceLines": [95, 98] },
        { "id": "spec-permissions", "label": "Let it call Retrieve", "cue": "“Without the alwaysAllow rule every retrieval prompts — which is never what you want in a RAG loop.”", "script": "Without the alwaysAllow rule every retrieval prompts — which is never what you want in a RAG loop. [[pause:900ms]] Watch lines 99 through 103 of 06 rag pipeline.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../06-rag-pipeline.md", "sourceLines": [99, 103] },
        { "id": "spec-indexing", "label": "Add the chunker and the corpus", "cue": "“Fixed 400-character windows over two inline documents. The corpus is spec surface, so what you indexed is reviewable in the same diff.”", "script": "Fixed 400-character windows over two inline documents. The corpus is spec surface, so what you indexed is reviewable in the same diff. [[pause:900ms]] Watch lines 104 through 118 of 06 rag pipeline.md become the next block of live slash crewhaus.yaml. Because the bytes come from the checked-in source, the onscreen result is the exact material the verifier exercises.", "action": "type", "source": "../../06-rag-pipeline.md", "sourceLines": [104, 118] }
      ]
    },
    {
      "title": "Beat 2 · Compile, don't trust",
      "beats": [
        { "id": "prove-lint", "label": "Lint the spec", "cue": "“Parse plus the IR passes the compile path skips — nothing emitted yet.”", "script": "Parse plus the IR passes the compile path skips — nothing emitted yet. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. Lint: clean — no findings.", "action": "command", "command": "crewhaus lint live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 0, "note": "lint: clean — no findings" } },
        { "id": "prove-ir", "label": "Show the lowered IR", "cue": "“The spec is sugar; this JSON is the component DAG the compiler actually sees.”", "script": "The spec is sugar; this JSON is the component DAG the compiler actually sees. [[pause:1.5s]] No credentials are involved in this check; a clean exit confirms the local path behaves as described. 39 lines of IR for target pipeline, redirected to scratch — the next beat opens it.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml --emit-ir > live/ir.json", "verify": { "mode": "offline", "expectedExit": 0, "note": "39 lines of IR for target pipeline, redirected to scratch — the next beat opens it", "produces": ["live/ir.json"] } },
        { "id": "prove-open-ir", "label": "Open the IR", "cue": "“Embedder, backend, chunk knobs and the documents, lowered to one flat record.”", "script": "Embedder, backend, chunk knobs and the documents, lowered to one flat record. [[pause:1.2s]] The editor opens live slash ir.json now. The relevant implementation is now visible, so the claim can be inspected directly instead of taken on faith.", "action": "open", "file": "live/ir.json" },
        { "id": "prove-check", "label": "Compile the bundle — and check it", "cue": "“--check asserts the shape, installs the deps and boots it: GREEN, no key needed.”", "script": "--check asserts the shape, installs the deps and boots it: GREEN, no key needed. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. GREEN — assertion ok (4 anchors); install ok; boot gated at the provider credentials gate.", "action": "command", "command": "crewhaus compile live/crewhaus.yaml -o live/dist --check", "verify": { "mode": "offline", "expectedExit": 0, "note": "GREEN — assertion ok (4 anchors); install ok; boot gated at the provider credentials gate", "produces": ["live/dist/agent.ts", "live/dist/package.json"] } },
        { "id": "prove-open-bundle", "label": "Open the emitted agent", "cue": "“The corpus is baked in as a literal, right next to the chunker. That is why editing documents means a recompile.”", "script": "The corpus is baked in as a literal, right next to the chunker. That is why editing documents means a recompile. [[pause:1.2s]] The editor opens live slash dist slash agent.ts now. This is the source or generated artifact behind the current step; focus on the details the first chunk called out.", "action": "open", "file": "live/dist/agent.ts" }
      ]
    },
    {
      "title": "Beat 3 · Run it",
      "beats": [
        { "id": "run-reject", "label": "Try crewhaus run first", "cue": "“Deliberate rejection, exit 1: only cli and browser run straight from the spec. Pipeline is compile-first.”", "script": "Deliberate rejection, exit 1: only cli and browser run straight from the spec. Pipeline is compile-first. [[pause:1.5s]] The nonzero result is intentional: this command expects exit code 1. Measured: `crewhaus run supports target: cli or browser (got \"pipeline\")`.", "action": "command", "command": "crewhaus run live/crewhaus.yaml", "verify": { "mode": "offline", "expectedExit": 1, "note": "measured: `crewhaus run supports target: cli or browser (got \"pipeline\")`" } },
        { "id": "run-bundle", "label": "Boot the bundle", "cue": "[needs-key] “Indexing runs once at process start, then the chat loop serves Retrieve calls against that store.”", "script": "Indexing runs once at process start, then the chat loop serves Retrieve calls against that store. [[pause:1.8s]] The demo crosses into its live path at this point; with the required credentials loaded, the real runtime performs the work. Indexes offline (prints `[pipeline] indexed 2 chunks`), then needs Anthropic credentials for the loop.", "action": "command", "command": "bun live/dist/agent.ts", "verify": { "mode": "needs-key", "note": "indexes offline (prints `[pipeline] indexed 2 chunks`), then needs Anthropic credentials for the loop" } },
        { "id": "run-ask", "label": "Ask something the corpus covers", "cue": "[needs-key] “Retrieve first, then two sentences with [N] citations — the numbering is the position in the retrieval result.”", "script": "Retrieve first, then two sentences with [N] citations — the numbering is the position in the retrieval result. [[pause:1.2s]] The driver sends the next user turn into the active session: “What target shapes does crewhaus support?” This keeps the proof inside the current conversation, so any continuity in the answer is visible.", "action": "input", "text": "What target shapes does crewhaus support?", "verify": { "mode": "needs-key" } },
        { "id": "run-refuse", "label": "Ask something it can't know", "cue": "[needs-key] “Retrieve has no relevance floor — it still hands back the two nearest chunks. The refusal is the prompt's job, and this is where you find out whether it holds.”", "script": "Retrieve has no relevance floor — it still hands back the two nearest chunks. The refusal is the prompt's job, and this is where you find out whether it holds. [[pause:1.2s]] The driver sends the next user turn into the active session: “What's the capital of France?” Expect \"I can only answer questions about the indexed docs.\".", "action": "input", "text": "What's the capital of France?", "verify": { "mode": "needs-key", "note": "expect \"I can only answer questions about the indexed docs.\"" } },
        { "id": "run-exit", "label": "Exit the loop", "cue": "[needs-key] “exit” returns to the shell.", "script": "“exit” returns to the shell. [[pause:1.2s]] The driver sends exit to close the active REPL cleanly. The terminal returns to a normal shell while the generated artifacts and session history remain available.", "action": "input", "text": "exit", "verify": { "mode": "needs-key" } }
      ]
    },
    {
      "title": "One more thing · N is the denominator for k",
      "beats": [
        { "id": "extra-boot-nokey", "label": "Boot with the key scrubbed", "cue": "“Same bundle, no credentials at all — chunk, embed and store still run to completion before anything asks the model for anything.”", "script": "Same bundle, no credentials at all — chunk, embed and store still run to completion before anything asks the model for anything. [[pause:1.5s]] Do not treat this rejection as a broken demo; exit code 1 is the expected outcome. Measured exit 1: the pipeline indexes, then the loop hits the provider credentials gate. Scrubbed explicitly because the Setup group already sourced .env into this shell.", "action": "command", "command": "env -u ANTHROPIC_API_KEY -u ANTHROPIC_AUTH_TOKEN bun live/dist/agent.ts > live/boot.log 2>&1", "verify": { "mode": "offline", "expectedExit": 1, "note": "measured exit 1: the pipeline indexes, then the loop hits the provider credentials gate. Scrubbed explicitly because the Setup group already sourced .env into this shell.", "produces": ["live/boot.log"] } },
        { "id": "extra-indexed", "label": "Read the chunk count", "cue": "“`indexed 2 chunks` is the denominator. defaultK: 2 against N=2 hands the model the whole store on every question — grow the corpus before you grow k.”", "script": "`indexed 2 chunks` is the denominator. defaultK: 2 against N=2 hands the model the whole store on every question — grow the corpus before you grow k. [[pause:1.5s]] This command is replayable offline, so its result is part of the repository's machine-checked demo contract. The boot line `[pipeline] indexed 2 chunks` — the recipe's Tuning-chunks section is about this number.", "action": "command", "command": "grep -n \"indexed\" live/boot.log", "verify": { "mode": "offline", "expectedExit": 0, "note": "the boot line `[pipeline] indexed 2 chunks` — the recipe's Tuning-chunks section is about this number" } }
      ]
    }
  ]
}
